# Evals для правила `create-fr` (опционально)

Файл [evals.json](evals.json) содержит 2–3 содержательных промпта для проверки [.cursor/rules/create-fr.mdc](../.cursor/rules/create-fr.mdc).

Парные прогоны (с правилом / baseline), `generate_review.py`, `grading.json` и оптимизация `description` через `run_loop.py` выполняются вне репозитория — см. установленный **skill-creator** и его инструкции. При отсутствии дисплея у viewer используй `--static`.

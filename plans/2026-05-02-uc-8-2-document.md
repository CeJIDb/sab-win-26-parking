# Подплан Фазы 1: UC-8.2 — детальный сценарий

**Дата**: 2026-05-02
**Родительский план**: [2026-05-02-uc-8-2-edo-integration.md](2026-05-02-uc-8-2-edo-integration.md)
**Задача**: написать детальное описание use case UC-8.2 «Создать договор с ЮЛ» с подписанием через ЭДО — единственный артефакт этой фазы.
**Время**: ~2.5–3 ч

## Зачем именно так

Этот UC — основа для всех последующих 8 deliverables ДЗ (ФТ, регламент, Sequence, JSON, XML, Postman, Swagger). Решения, зафиксированные здесь по сценарию, акторам, потоку и исключениям, будут переиспользоваться и обязаны быть консистентны во всех артефактах.

UC пишется по образцу [uc-10-2-pay-online-short-term-rental.md](../docs/artifacts/use-case/uc-10-2-pay-online-short-term-rental.md) — тот же набор разделов, та же стилистика нумерованных шагов. Дополнительный референс по содержательной детализации шагов и атрибутам сущностей — [uc-5-5-create-notification.md](../docs/artifacts/use-case/uc-5-5-create-notification.md).

**Формат расширений** — по образцу [uc-12-1](../docs/artifacts/use-case/uc-12-1-pass-auto-identification-entry.md): заголовок ситуации `**Nа. Описание**` (жирным), подшаги `Nа1.`, `Nа2.`, …, вложенные ситуации `Nа2а.`, `Nа2б.` с подшагами `Nа2а1.`, `Nа2б1.` Кириллические буквы (`а`, `б`, `в`), без точек между числом и буквой, точка только в конце номера. Формат `N.а.` / `N.а.1.` из uc-7-\* и `1a / 1a 1` из uc-10-2 — **не используем**.

Строку `**ID**` из таблицы метаданных **убираем** (в проекте UC идентифицируется кодом из реестра — отдельный ID не нужен).

## Решения, зафиксированные перед стартом

Согласовано в чате 2026-05-02:

| #   | Вопрос                               | Решение                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Актор и точка входа                  | Только UC-8.2: Клиент ЮЛ инициирует из ЛК (UC-8.9 в админке — отдельный UC, не входит)                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2   | Бизнес-кейс                          | Долгосрочная аренда машиномест по типовому шаблону                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3   | Порядок подписания                   | **Сценарий Б с маршрутом согласования в ЭДО.** Платформа отправляет документ в ЭДО **один раз** с маршрутом «клиент → Владелец». ЭДО сам управляет очередностью, уведомляет Владельца в его кабинете, хранит промежуточные подписи. Наша система не таскает частично подписанный документ — реагирует на **события статусов** и забирает только финальный документ с двумя ЭП. Владелец подписывает в том же внешнем ЭДО (E12), что и клиент (либо через роуминг). Подтверждено TO-BE-интервью 3 |
| 4   | Обработка асинхрона ожидания подписи | Подписка на события ЭДО (`document.signed.by.counterparty`, `document.fully.signed`, `document.rejected`, `document.expired`). Webhook как основной + polling как fallback. Применяется к двум событиям: подпись клиента и финальная подпись Владельца                                                                                                                                                                                                                                           |
| 5   | Покрываемые исключения               | Отказ клиента, таймаут подписи клиента, отказ Владельца, таймаут подписи Владельца, недоступность ЭДО, отсутствие роуминга / контрагент не подключен, идемпотентность повторов. **Просроченный сертификат — исключаем** (внутренняя ошибка ЭДО, нам не виден отдельно)                                                                                                                                                                                                                           |
| 6   | Граница UC                           | UC завершается на получении документа, подписанного обеими сторонами. Полный жизненный цикл — часы/дни, маршрут согласования живет внутри ЭДО                                                                                                                                                                                                                                                                                                                                                    |

DADATA-интеграция и UC-8.5/8.4 в scope не входят.

## Цель

Один файл: `docs/artifacts/use-case/uc-8-2-create-contract-legal-entity.md`. Ровно по образцу uc-10-2, но про создание договора с ЮЛ через ЭДО.

## Структура UC-документа

Воспроизводим структуру [uc-10-2](../docs/artifacts/use-case/uc-10-2-pay-online-short-term-rental.md):

1. **Заголовок:** `# UC-8.2 Создать договор с ЮЛ (долгосрочная аренда)`
2. **Оглавление**
3. **Метаданные** (таблица: Автор, Название, Действующее лицо — без строки `ID`)
4. **Предусловия**
5. **Триггер**
6. **Постусловие**
7. **Основной поток** (нумерованные шаги; атрибуты `[договор]` — по концептуальной модели, формат camelCase на русском)
8. **Расширение** (альтернативные потоки и исключения)
9. **Требования для UC-8.2 → интеграции** (список `INT-*` — заполнится после Фазы 2)
10. **Связанные документы**

**Отступление от родительского плана**: дублирование архитектурного допущения про «локальный ЭДО Adapter» в шапке UC **не делаем** — уровень абстракции UC не должен упоминать внутренние модули. Допущение остается в [родительском плане](2026-05-02-uc-8-2-edo-integration.md), Sequence (Фаза 4) и регламенте (Фазы 3/9).

## Содержательный каркас (что именно пишем)

### Действующие лица

- **Клиент ЮЛ** — инициатор, у которого уже есть профиль в системе, КЭП в кабинете ЭДО.
- **Система (платформа)** — оркестратор: генерирует договор, отправляет в ЭДО с маршрутом подписания, реагирует на события статусов от ЭДО, забирает финальный документ, создает бронирования.
- **Владелец** — активный актор со стороны платформы: подписывает договор своей КЭП в кабинете того же внешнего ЭДО (E12), что и клиент (или через роуминг). **Уведомление о необходимости подписать ему отправляет ЭДО, не наша система** — Владелец работает в кабинете ЭДО. TO-BE-решение, см. [interview-3](../docs/interviews/protocols/interview-protocol-3-2026-02-04-v01.md).
- **ЭДО (E12)** — внешний оператор (СБИС/Диадок) для подписания и обмена документами; здесь же оркестрирует **маршрут согласования** клиент→Владелец.
- **Сервис уведомлений (E8)** — внешний канал для информирования клиента и Управляющего (Владельца не уведомляем — это делает ЭДО).
- **Управляющий** — постусловный наблюдатель (получает уведомление о новом подписанном договоре). В подписании не участвует.

### Предусловия (черновой список, дошлифуется при написании)

- Клиент ЮЛ авторизован в ЛК.
- В профиле клиента заполнены **данные организации** (UC-2.13 «Добавить данные организации»).
- В профиле клиента заполнены **банковские реквизиты организации** (хотя бы один счет, отмеченный как основной).
- В профиле клиента есть хотя бы одно **ТС** (UC-3.1 «Добавить ТС»).
- Клиент подключен к одному из операторов ЭДО, поддерживаемых платформой (или есть роуминг с тем оператором, где работает Владелец).
- У Владельца есть аккаунт и действующая КЭП в том же операторе ЭДО (или совместимом через роуминг) для подписания договоров со стороны платформы.
- Существует опубликованный шаблон договора долгосрочной аренды.
- Доступны для бронирования машиноместа в нужном секторе на нужный срок.

Конкретный состав полей «данных организации» и «банковских реквизитов» в UC **не дублируем** — отсылаемся к концептуальной модели ([Организация](../docs/artifacts/conceptual-model-with-attributes.md), сущность «Организация» и «Банковские реквизиты организации»). В предусловии ограничиваемся формулировкой «заполнены данные организации / банковские реквизиты». Атрибуты сверяли с ERD ([organizations / organization_bank_accounts](../docs/architecture/database/erd/erd-normalized-er-model.md)) — расхождений нет.

КЭП организации в предусловиях **не фигурирует** — подписание делает Владелец своей КЭП в кабинете ЭДО (Сценарий Б, см. решение №3).

### Триггер

Клиент ЮЛ в ЛК переходит в раздел «Создать договор».

Заполнение формы (срок, тариф, ТС, машиноместа) и подтверждение «Заключить договор» — это **шаги основного потока**, а не часть триггера. Триггер фиксирует только момент инициации сценария.

Поля формы — по [contracts-create.njk](../ui/templates/pages/contracts-create.njk):

- **Срок действия договора**: дата начала, дата окончания.
- **Тариф по договору**: выбор из доступных опубликованных тарифов долгосрочной аренды.
- **ТС по договору**: мультивыбор из ТС в профиле клиента (`vehicles.client_id`).
- **Машиноместа по договору**: мультивыбор из доступных к бронированию ПМ.

Реквизиты ЮЛ в форме **не запрашиваются** — берутся из профиля (см. предусловия).

### Постусловие (целевое финальное состояние)

- `[договор].статус` = `активен`.
- `[договор].файлДоговора` указывает на сохраненный финальный документ с двумя ЭП.
- `[договор].датаПодписанияКлиентом` и `[договор].датаПодписанияПарковкой` заполнены.
- Клиент получил уведомление об успешном заключении договора и доступности в ЛК.
- Управляющий получил уведомление о новом договоре.
- На указанные клиентом машиноместа созданы бронирования на весь срок договора.

### Атрибуты `[договор]` (используем в шагах)

Имена атрибутов берем из концептуальной модели ([Договор](../docs/artifacts/conceptual-model-with-attributes.md)) — формат `camelCase` на русском. В шагах используем тот же стиль (без латинских и без «idВнешнегоЭДО»-смесей).

| Атрибут                   | Когда заполняется                                                                                                                                |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `идентификатор`           | автоген при создании (шаг 4)                                                                                                                     |
| `клиентИд`                | ссылка на клиента из сессии (шаг 4)                                                                                                              |
| `шаблонДоговораИд`        | ссылка на опубликованный шаблон долгосрочной аренды для ЮЛ (шаг 4)                                                                               |
| `номерДоговора`           | автоген по правилу нумерации (шаг 4)                                                                                                             |
| `датаНачала`              | из формы (шаг 4)                                                                                                                                 |
| `датаОкончания`           | из формы (шаг 4)                                                                                                                                 |
| `статус`                  | переходы: `черновик` (4) → `на подписании клиентом` (8) → `на подписании парковкой` (11) → `активен` (14). Альтернативные ветки — см. Расширение |
| `файлДоговора`            | ссылка на финальный документ ЭДО (шаг 14); до этого `null`                                                                                       |
| `датаПодписанияКлиентом`  | момент события «подписан клиентом» из ЭДО (шаг 11)                                                                                               |
| `датаПодписанияПарковкой` | момент получения финального документа с двумя ЭП (шаг 14)                                                                                        |

**Соответствие с моделью.** Для UC расширены [Enum «Статус договора»](../docs/artifacts/conceptual-model-with-attributes.md) (добавлены `на подписании клиентом`, `на подписании парковкой`, `отклонен клиентом`, `отклонен парковкой`, `просрочен`) и [сущность «Договор»](../docs/artifacts/conceptual-model-with-attributes.md) (добавлены атрибуты `датаПодписанияКлиентом`, `датаПодписанияПарковкой`). Правки сделаны в этой же фазе; запись `CHG-*` в журнал трассировки делает мастер-план в Фазе 10. ERD синхронизирован параллельной задачей 1Б — см. итоговый блок плана.

### Основной поток (черновая структура — финал в самом файле)

**Принципы**:

- Описываем только то, что наблюдает система. Действия в кабинете ЭДО (Клиент открывает документ и подписывает; Владелец открывает документ и подписывает) системе не видны — шагами не описываются.
- Маршрут согласования живет в ЭДО. Платформа отправляет документ **один раз** с маршрутом «клиент → Владелец», далее реагирует на события статусов и забирает финальный документ. Промежуточно подписанный документ к нам не приезжает.
- Уведомление Владельца о необходимости подписать — за ЭДО (его кабинет уведомляет автоматически). Наша система Владельцу ничего не шлет.

Уровень абстракции — UC: единственный системный актор «Система». Внутренние модули (Pricing / Facility / Contract Service / ЭДО Adapter) в шагах не упоминаем — это уровень Sequence (Фаза 4). Формат документа (XML) тоже опускаем — это уровень XSD/Sequence. **Принцип: один шаг — одно действие.** Атрибуты `[договор]` — по таблице выше.

1. Клиент в форме «Создать договор» указывает параметры аренды: срок (`датаНачала`, `датаОкончания`), тариф из доступных, ТС из профиля, машиноместа.
2. Система рассчитывает стоимость по выбранному клиентом тарифу и отображает условия (сумма, период, состав ПМ, ТС).
3. Клиент подтверждает «Заключить договор» → [Ok].
4. Система создает `[договор]` со следующими атрибутами:
   - `идентификатор` — автоген;
   - `клиентИд` — ссылка на клиента из сессии;
   - `шаблонДоговораИд` — ссылка на опубликованный шаблон долгосрочной аренды для ЮЛ;
   - `номерДоговора` — автоген по правилу нумерации;
   - `датаНачала`, `датаОкончания` — из формы;
   - `статус` = `черновик`;
   - `файлДоговора` = `null`.
5. Система формирует документ договора по `шаблонуДоговора` с подстановкой реквизитов ЮЛ из профиля клиента и параметров из формы.
6. Система передает документ в ЭДО **с маршрутом согласования: клиент → Владелец**.
7. Система получает от ЭДО подтверждение приема документа с идентификатором документа в ЭДО.
8. Система обновляет `[договор]`: `статус` = `на подписании клиентом`.
9. Система отправляет клиенту уведомление «договор отправлен на подпись в [ЭДО]».
10. Система получает от ЭДО событие «документ подписан клиентом» (промежуточно подписанный документ к нам не приезжает).
11. Система обновляет `[договор]`: `статус` = `на подписании парковкой`, `датаПодписанияКлиентом` = момент события из ЭДО.
12. Система получает от ЭДО документ, подписанный обеими сторонами (с двумя ЭП).
13. Система сохраняет финальный документ во внешнем хранилище.
14. Система обновляет `[договор]`: `статус` = `активен`, `файлДоговора` = ссылка на сохраненный документ, `датаПодписанияПарковкой` = момент получения финального документа.
15. Система создает бронирования на указанные клиентом машиноместа на весь срок договора.
16. Система отправляет клиенту уведомление об успешном заключении договора.
17. Система отправляет управляющему уведомление о новом договоре.
18. Сценарий завершается.

### Расширение (исключения по решению из п.5)

Формат — `Nа.` (заголовок жирным) / `Nа1.`, `Nа2.` (подшаги) по образцу [uc-12-1](../docs/artifacts/use-case/uc-12-1-pass-auto-identification-entry.md). Нумерация привязана к шагам основного потока (1–18): расширение `Nа.` — альтернативный исход шага N. Все исключения, связанные с подписанием, формулируем как **события из ЭДО** — мы не наблюдаем причину напрямую, видим только статус. В плане фиксируем заголовки ситуаций; полные подшаги (`Nа1.`, `Nа2.` …) пишем в самом файле UC.

Имена статусов в расширениях — из расширенного enum концептуальной модели: `отклонен клиентом`, `отклонен парковкой`, `просрочен` для терминальных неуспешных веток.

**SLA подписи**: в `10б` и `12б` используем **7 календарных дней** на каждый этап подписи (клиент → Владелец). Заказчик SLA не озвучивал; число выбрано командой 2026-05-02 на основе типичной практики российских ЭДО (Диадок/СБИС: 5–10 дней). Параметр выносится в конфигурацию, при появлении заказчика — уточнить.

- **3а.** Идемпотентность по бизнес-ключу: если у клиента уже есть `[договор]` в статусе `черновик`, `на согласовании`, `на подписании клиентом` или `на подписании парковкой` с тем же `шаблономДоговораИд` и пересекающимся составом ПМ за пересекающийся период — Система не создает новый, а возвращает существующий с его текущим статусом.
- **6а.** ЭДО недоступно при отправке документа (сетевая ошибка / таймаут запроса) — `[договор]` остается в `черновик`, клиент уведомляется об ошибке отправки.
- **7а.** Подтверждение приема от ЭДО не получено (таймаут ответа / некорректный ответ без идентификатора документа) — `[договор]` остается в `черновик`, клиент уведомляется об ошибке отправки.
- **10а.** Из ЭДО получено событие отказа клиента от подписания — `[договор].статус` = `отклонен клиентом`, клиент уведомляется.
- **10б.** Событие подписания клиентом не получено в течение 7 календарных дней с момента отправки в ЭДО — `[договор].статус` = `просрочен`, клиент уведомляется.
- **12а.** Из ЭДО получено событие отказа Владельца от подписания — `[договор].статус` = `отклонен парковкой`, клиент уведомляется.
- **12б.** Финальный документ не получен в течение 7 календарных дней с момента подписи клиента — `[договор].статус` = `просрочен`, клиент уведомляется.

### Требования для UC-8.2 → интеграции

Раздел заполнится после Фазы 2 — список `INT-*`, которые мы заведем в integration-requirements.md.

### Связанные документы (что здесь будем линковать)

В разделе UC ссылки даем **только на существующие файлы** — иначе `lint:md-links` упадет на несуществующих путях. Артефакты будущих фаз указываем текстом без ссылок; ссылки добавим в Фазе 10.

Линкуем сразу:

- [Реестр use case](use-case-registry.md), [индекс UC](readme.md).
- [Требования к интеграции](../../specs/integration/integration-requirements.md) — общий список `INT-*`; конкретные требования для UC-8.2 появятся в Фазе 2.
- Родительский план [2026-05-02-uc-8-2-edo-integration.md](../../../plans/2026-05-02-uc-8-2-edo-integration.md).

Упоминаем без ссылок (добавим в Фазе 10):

- ФТ интеграции (Фаза 2), регламент v1/v2 (Фазы 3/9), UML Sequence (Фаза 4), JSON+Schema (Фаза 5), XML+XSD (Фаза 6).

**Граничное условие**: создание бронирований шагом 15 — это часть UC-8.2, не передача в UC-7.1. Межсервисный поток `Contract → Facility` упоминается в UC только бизнес-формулировкой «создает бронирования», без раскрытия модулей. В регламенте/ФТ как «третья внутренняя интеграция» **не выносится** — фиксируем только `Contract ↔ ЭДО Adapter` и `Contract → Notification`.

## Scope

**Входит:**

- Один UC-документ по структуре uc-10-2 (10 разделов, без дублирования архитектурного допущения).
- 6 решений из шапки плана отражены в потоке/расширениях.
- Основной поток (~18 шагов, принцип «один шаг — одно действие») + 7 расширений (`3а`, `6а`, `7а`, `10а`, `10б`, `12а`, `12б`).
- Расширение enum «Статус договора» и атрибутов сущности «Договор» в [концептуальной модели](../docs/artifacts/conceptual-model-with-attributes.md) — выполнено в этой же фазе.
- **Параллельная задача 1Б** (см. ниже): синхронизация ERD `contracts` с расширенным enum концептуальной модели.

**Не входит:**

- Детализация криптомеханики КЭП (HSM, КриптоПро) — упоминается только как факт.
- Конкретный вендор ЭДО (СБИС vs Диадок) — UC написан в нейтральных терминах «оператор ЭДО».
- ФТ, Sequence, JSON, XML, регламент — другие фазы.
- Правка реестра UC и диаграммы UC (UC-8.2 уже в реестре).
- TO-BE state machine договора с расширенным enum — артефакт **не трогаем** (договоренность с автором плана 2026-05-02).

## Definition of Done для Фазы 1

- [ ] Файл `docs/artifacts/use-case/uc-8-2-create-contract-legal-entity.md` создан.
- [ ] Все 10 разделов структуры заполнены (раздел `Требования → интеграции` оставляем заглушкой со ссылкой на Фазу 2). Шапки с архитектурным допущением про локальный ЭДО Adapter в UC **нет** (отступление от родительского плана).
- [ ] Основной поток содержит ~18 шагов (принцип «один шаг — одно действие») по Сценарию Б с маршрутом в ЭДО: заполнение → расчет → подтверждение → создание `[договор]` (`статус` = `черновик`) → формирование документа → передача в ЭДО → прием подтверждения → `статус` = `на подписании клиентом` → событие подписания клиентом → `статус` = `на подписании парковкой`, `датаПодписанияКлиентом` → финальный документ → сохранение → `статус` = `активен`, `файлДоговора`, `датаПодписанияПарковкой` → бронирования → уведомления. Уровень абстракции — UC: единственный системный актор «Система», без внутренних модулей и без формата документа (XML).
- [ ] В основном потоке нет шагов, описывающих действия в кабинете ЭДО, и **нет шага «уведомить Владельца»** — это делает ЭДО. Промежуточно подписанный документ к нам не приезжает.
- [ ] Атрибуты `[договор]` в шагах — только из концептуальной модели в формате camelCase на русском (`идентификатор`, `клиентИд`, `шаблонДоговораИд`, `номерДоговора`, `датаНачала`, `датаОкончания`, `статус`, `файлДоговора`, `датаПодписанияКлиентом`, `датаПодписанияПарковкой`). Никаких `idВнешнегоЭДО` / латинских смесей.
- [ ] Статусы `[договор]` — только из расширенного enum концептуальной модели (используются: `черновик`, `на подписании клиентом`, `на подписании парковкой`, `активен`, `отклонен клиентом`, `отклонен парковкой`, `просрочен`).
- [ ] Расширение покрывает 7 исключений (`3а.`, `6а.`, `7а.`, `10а.`, `10б.`, `12а.`, `12б.`) в формате uc-12-1 (`Nа.` / `Nа1.`, без точек между числом и буквой). Идемпотентность `3а` — по бизнес-ключу (договор клиента в любом «не финальном» статусе с тем же шаблоном и пересекающимися ПМ/периодом). SLA в `10б`/`12б` — 7 календарных дней.
- [ ] UC-2.13 «Добавить данные организации» в предусловиях упомянут **без markdown-ссылки** (файл-артефакт еще не создан, ссылка ляжет на `lint:md-links`).
- [ ] Концептуальная модель ([conceptual-model-with-attributes.md](../docs/artifacts/conceptual-model-with-attributes.md)) расширена: 5 новых значений в enum «Статус договора» + 2 новых атрибута в сущности «Договор». Прошла `npm run lint:md`.
- [ ] Markdown UC проходит `npm run lint:md`.
- [ ] Ссылки в файле проходят `npm run lint:md-links` (раздел `Связанные документы` ссылается только на существующие артефакты).
- [ ] Имя файла на латинице kebab-case (`npm run lint:file-names`).
- [ ] Текст без буквы «ё».
- [ ] Индекс `docs/artifacts/use-case/readme.md` обновлен (добавлена ссылка на UC-8.2).
- [ ] Запись `CHG-*` в [traceability-matrix-log.md](../docs/process/traceability-matrix-log.md) делает **мастер-план в Фазе 10**, не подплан Фазы 1 (одна запись на весь набор 9 артефактов + правка концептуальной модели).

## Параллельная задача 1Б: синхронизация ERD `contracts` с концептуальной моделью

**Запускается параллельно с потоком 1.1–1.7** (отдельным агентом в отдельном чате). Зависимостей с UC-документом нет: оба потока опираются на уже расширенную концептуальную модель (Шаг 1.0). Должна завершиться **до Фазы 4 (Sequence)** — там потребуется консистентность ERD с моделью.

### Файл

`docs/architecture/database/erd/erd-normalized-er-model.md` — единственный файл правки.

### Что сделать

1. **Расширить `contract_status_enum`** (строка ~1126, секция `### contracts`).

   Сейчас: `DRAFT, ACTIVE, EXPIRED, TERMINATED`.

   Стало (10 значений с маппингом на расширенную концептуальную модель):

   | ENUM (ERD)                  | Концептуальная модель     |
   | --------------------------- | ------------------------- |
   | `DRAFT`                     | `черновик`                |
   | `UNDER_REVIEW`              | `на согласовании`         |
   | `AWAITING_CLIENT_SIGNATURE` | `на подписании клиентом`  |
   | `AWAITING_OWNER_SIGNATURE`  | `на подписании парковкой` |
   | `ACTIVE`                    | `активен`                 |
   | `SUSPENDED`                 | `приостановлен`           |
   | `REJECTED_BY_CLIENT`        | `отклонен клиентом`       |
   | `REJECTED_BY_OWNER`         | `отклонен парковкой`      |
   | `EXPIRED`                   | `просрочен`               |
   | `TERMINATED`                | `расторгнут`              |
   | `ARCHIVED`                  | `архив`                   |

   Решения по маппингу: `EXPIRED` (уже в enum) переиспользуется для `просрочен` (договор не вступил в силу из-за таймаута подписи); `TERMINATED` остается для `расторгнут` (действующий договор прекращен после `активен`). Это семантически разные ветки — не объединять.

2. **Добавить две колонки в таблицу `contracts`** (после `document_file_ref`, перед `created_at`):

   | Колонка            | Тип PostgreSQL | Источник в концептуальной модели |
   | ------------------ | -------------- | -------------------------------- |
   | `client_signed_at` | `TIMESTAMPTZ`  | `датаПодписанияКлиентом`         |
   | `owner_signed_at`  | `TIMESTAMPTZ`  | `датаПодписанияПарковкой`        |

   Обе nullable (договор может быть в `черновик`/`UNDER_REVIEW` без подписей).

3. **Обновить mermaid-описание** `contracts` (строки ~373–382) — добавить `datetime client_signed_at` и `datetime owner_signed_at`.

4. **Обновить сводную таблицу** «Сущности и ключевые поля» (строка ~644 в обзорной части): пометить расширение enum.

5. **Проверить, нет ли в файле других мест**, где упоминается `contract_status_enum` со старым перечислением (например, в DDL-блоках, sample-запросах, comments). Привести в соответствие.

### DoD параллельной задачи 1Б

- [x] `contract_status_enum` в [erd-normalized-er-model.md](../docs/architecture/database/erd/erd-normalized-er-model.md) содержит 11 значений по таблице маппинга выше.
- [x] Таблица `contracts` имеет колонки `client_signed_at` и `owner_signed_at` (`TIMESTAMPTZ`, nullable).
- [x] Mermaid-блок `contracts` синхронизирован с DDL-таблицей.
- [x] Все упоминания старого набора (`DRAFT, ACTIVE, EXPIRED, TERMINATED`) в файле найдены и обновлены.
- [x] `npm run lint:md` зеленый.
- [x] Текст без буквы «ё».
- [ ] Запись `CHG-*` в [traceability-matrix-log.md](../docs/process/traceability-matrix-log.md) делает мастер-план в Фазе 10 (одна общая запись на UC-8.2 + правки модели + правки ERD).

### Что НЕ делать

- **Не трогать словарь данных** ([data-dictionary.md](../docs/architecture/database/erd/data-dictionary.md)) — статус `imported-working-material`, правится отдельно.
- **Не создавать TO-BE state machine** — этот артефакт по договоренности не трогаем.
- Не править миграции / SQL DDL вне ERD-документа — миграции делаются позже, отдельным контуром.
- Не править концептуальную модель — она уже расширена в Шаге 1.0 основного потока.

## Фазы и статус

- [x] Шаг 1.0. Расширить концептуальную модель: enum «Статус договора» (+5 значений) и атрибуты сущности «Договор» (+2 атрибута). Сделано до написания UC, чтобы шаги ссылались на актуальную модель.
- [x] Шаг 1.1. Создать файл-скелет с метаданными и оглавлением (без шапки с архитектурным допущением — см. отступление от родителя в «Структуре UC-документа»).
- [x] Шаг 1.2. Заполнить Предусловия / Триггер / Постусловие. UC-2.13 в предусловиях — текстом, без ссылки. Постусловие сформулировать через `[договор].статус` = `активен`, `[договор].файлДоговора` и заполненные `датаПодписанияКлиентом` / `датаПодписанияПарковкой`.
- [x] Шаг 1.3. Написать Основной поток (~18 шагов, принцип «один шаг — одно действие»): заполнение → расчет → подтверждение → создание `[договор]` (`черновик`) → формирование документа → передача в ЭДО → прием подтверждения → `на подписании клиентом` → событие подписания клиента → `на подписании парковкой` + `датаПодписанияКлиентом` → финальный документ → сохранение → `активен` + `файлДоговора` + `датаПодписанияПарковкой` → бронирования → уведомления. Атрибуты и статусы — только из концептуальной модели. Единственный системный актор — «Система»; Владелец — внешний. Уведомление Владельца на нашей стороне отсутствует. Действия в кабинете ЭДО не описываем.
- [x] Шаг 1.4. Написать Расширение (7 исключений: `3а.`, `6а.`, `7а.`, `10а.`, `10б.`, `12а.`, `12б.`; формат `Nа.` / `Nа1.` по uc-12-1). `3а` — идемпотентность по бизнес-ключу. В `10б`/`12б` SLA = 7 календарных дней. Терминальные статусы: `отклонен клиентом`, `отклонен парковкой`, `просрочен`.
- [x] Шаг 1.5. Заполнить Связанные документы (только существующие файлы; раздел `Требования → интеграции` — заглушка до Фазы 2).
- [x] Шаг 1.6. Обновить индекс [docs/artifacts/use-case/readme.md](../docs/artifacts/use-case/readme.md).
- [x] Шаг 1.7. Прогнать `npm run lint:md`, `npm run lint:md-links`, `npm run lint:file-names`. Поправить замечания.

Запись `CHG-*` в журнал трассировки делает мастер-план в Фазе 10, не подплан.

## Итог

Подплан создан 2026-05-02. Содержит два независимых потока:

- **Основной поток (Шаги 1.0–1.7)**: написание UC-документа `uc-8-2-create-contract-legal-entity.md`.
- **Параллельная задача 1Б**: синхронизация ERD `contracts` с расширенной концептуальной моделью.

Заполнится по ходу: какие шаги завершены, какие решения приняты при написании, какие нюансы вылезли.

**Сделано в этой фазе вне UC-документа** (для записи `CHG-*` в Фазе 10):

- [conceptual-model-with-attributes.md](../docs/artifacts/conceptual-model-with-attributes.md): расширен `Enum: Статус договора` (+5 значений: `на подписании клиентом`, `на подписании парковкой`, `отклонен клиентом`, `отклонен парковкой`, `просрочен`); добавлены атрибуты `датаПодписанияКлиентом` и `датаПодписанияПарковкой` в сущность `Договор`. Источник: исследование best practices CLM (Juro, Sievo) + статусы российских ЭДО (Диадок API, СБИС API).
- [erd-normalized-er-model.md](../docs/architecture/database/erd/erd-normalized-er-model.md) — параллельная задача 1Б выполнена 2026-05-02:
  - `contract_status_enum` расширен до 11 значений по таблице маппинга (DDL `contracts`, ~стр. 1118–1133): `DRAFT`, `UNDER_REVIEW`, `AWAITING_CLIENT_SIGNATURE`, `AWAITING_OWNER_SIGNATURE`, `ACTIVE`, `SUSPENDED`, `REJECTED_BY_CLIENT`, `REJECTED_BY_OWNER`, `EXPIRED`, `TERMINATED`, `ARCHIVED`.
  - Добавлены колонки `client_signed_at TIMESTAMPTZ` и `owner_signed_at TIMESTAMPTZ` (nullable, после `document_file_ref`) в DDL `contracts` и mermaid-блок (~стр. 373–384).
  - Сводная таблица «Сущности и ключевые поля» (~стр. 646) и Section 25 «contracts — Договор» (~стр. 1980–1988) обновлены.
  - Найдено 3 упоминания `contract_status_enum`: DDL `contracts` + 2 в `contract_status_history` (ссылки на тип, обновляются автоматически). Sample-запросов по статусам `contracts` в файле нет.
  - `npm run lint:md` зеленый, буква «ё» отсутствует.
  - Не правились: `data-dictionary-buildin-import.md`, концептуальная модель, миграции, TO-BE state machine.

### Постскриптум 2026-05-02 (решения позднее, после Фаз 2–3 мастер-плана)

Тело подплана выше сохранено как историческая фиксация решений на момент написания UC-8.2. Что изменилось позднее и почему:

- **Учебное допущение «локальный ЭДО Adapter» отменено.** В исходном подплане оно фигурировало в нескольких местах (формат расширений, описание шагов, DoD, Scope) как часть архитектуры UC-8.2. После старта Фаз 2 и 3 решено отказаться от адаптера: поток обмена с E12 ЭДО в UML Sequence (Фаза 4) и других архитектурных артефактах идет напрямую от `Сервис Договоров` (P14) — как в [DFD L1](../docs/artifacts/dfd-l1.md) F03/F04. На сам файл UC-8.2 это не повлияло (UC и так не упоминал адаптер); правки фактически в шапке мастер-плана и в семействе деривативных артефактов Фаз 4–9.
- **Расширение `6а` UC-8.2 удалено.** При синхронизации с buildin.ai (`CHG-20260502-004`) ветка `6а. ЭДО недоступно при отправке документа` была убрана; фактический набор расширений в [uc-8-2-create-contract-legal-entity.md](../docs/artifacts/use-case/uc-8-2-create-contract-legal-entity.md) — 6 расширений (`3а`, `7а`, `10а`, `10б`, `12а`, `12б`), не 7. В теле этого подплана выше упоминается «7 расширений» — это устаревшая запись на момент написания.
- **`INT-016` / `INT-017` удалены из требований** (`CHG-20260502-005`) — это касается подплана Фазы 2, не Фазы 1, но общая мотивация одна: отказ от учебного допущения про адаптер и outbox как отдельных требований. Outbox-механика остается на уровне архитектуры (Sequence) по [ADR-003](../docs/architecture/adr/adr-003-modular-monolith.md) инв. 4.

См. [мастер-план UC-8.2](2026-05-02-uc-8-2-edo-integration.md) (актуальное состояние решений) и `CHG-20260502-005` в [журнале трассировки](../docs/process/traceability-matrix-log.md).

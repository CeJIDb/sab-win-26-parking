# Подплан Фазы 4: UC-8.2 — UML Sequence (Mermaid + PlantUML)

**Дата**: 2026-05-02
**Родительский план**: [2026-05-02-uc-8-2-edo-integration.md](2026-05-02-uc-8-2-edo-integration.md)
**Задача**: создать [docs/architecture/integration/sequence-uc-8-2-create-contract.md](../docs/architecture/integration/sequence-uc-8-2-create-contract.md) — UML Sequence для сценария UC-8.2 «Создать договор с ЮЛ через ЭДО» в двух нотациях: inline Mermaid (как [UC-12.1](../docs/architecture/integration/sequence-uc-12-1-pass-auto-identification-entry.md)) и PlantUML (`.puml`-исходник + PNG-экспорт). Диаграмма покрывает основной поток (~18 шагов) и 6 расширений (`3а`, `7а`, `10а`, `10б`, `12а`, `12б`).
**Время**: ~1.5–2 ч (с учетом двух нотаций и PNG-экспорта)

## Зачем именно так

Sequence — четвертый deliverable из 9 в наборе UC-8.2. Он опирается на уже зафиксированные в Фазах 1–3 артефакты: бизнес-сценарий [UC-8.2](../docs/artifacts/use-case/uc-8-2-create-contract-legal-entity.md), требования [INT-014 / INT-015](../docs/specs/integration/integration-requirements.md), [Блок 3 регламента](../docs/architecture/integration/is-interaction-regulation.md). Дает картину «как именно сообщения движутся между PWA, Сервисом Договоров, БД, Notification Service / Adapter, ЭДО и Сервисом уведомлений во времени», которая в UC и регламенте отсутствует.

Стиль и оформление воспроизводят [UC-12.1 Sequence](../docs/architecture/integration/sequence-uc-12-1-pass-auto-identification-entry.md) — единственный Mermaid-образец в проекте (другой Sequence — UC-10.2 — это draw.io PNG, не Mermaid). Outbox-механика для уведомлений показывается явно, как в UC-12.1, через `INSERT outbox_events (...)` + `CDC / Outbox Polling`. Это согласуется с [ADR-003](../docs/architecture/adr/adr-003-modular-monolith.md) инв. 4 (transactional outbox для развязки модулей).

PlantUML добавляется как вторая нотация на тот же сценарий — учебная цель курса (показать обе нотации UML Sequence на одном артефакте). PlantUML в проекте раньше не использовался, поэтому подплан фиксирует и инфраструктурную часть: расположение `.puml`-исходника, PNG-экспорт, ручной рендер через [plantuml.com/plantuml](https://plantuml.com/plantuml).

## Решения, зафиксированные перед стартом

Согласовано в чате 2026-05-02 (по итогам блоков 1–5 уточняющих вопросов):

| #   | Вопрос                                                                                        | Решение                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Инициатор сценария                                                                            | PWA Клиента ЮЛ + API Gateway (по триггеру UC-8.2 «Клиент ЮЛ в ЛК переходит в раздел Создать договор»). Admin Panel из участников исключена — она относится к UC-8.9, не к UC-8.2. Запись «Admin Panel UI» в исходной формулировке мастер-плана была опечаткой                                                                                                                                                                                                                           |
| 2   | Полнота диаграммы                                                                             | Один Sequence на весь UC-8.2: основной поток + 6 расширений в `break` / `opt` / `alt`-блоках, по образцу UC-12.1                                                                                                                                                                                                                                                                                                                                                                        |
| 3   | Client Profile (P3)                                                                           | Включается явно как read-источник реквизитов ЮЛ для шага 5 UC-8.2 (формирование документа). Чтение через именованную SQL view, симметрично [ADR-005](../docs/architecture/adr/adr-005-access-control-direct-db-read.md)                                                                                                                                                                                                                                                                 |
| 4   | Локальный ЭДО Adapter                                                                         | Удален из всех артефактов UC-8.2. Поток обмена с E12 ЭДО идет напрямую от `Сервис Договоров`, как в [DFD L1](../docs/artifacts/dfd-l1.md) F03/F04. См. `CHG-20260502-005`                                                                                                                                                                                                                                                                                                               |
| 5   | Outbox для уведомлений                                                                        | Воспроизводится по образцу UC-12.1: явные `INSERT outbox_events (aggregate=CONTRACT, type=...)` от `Сервис Договоров`, асинхронно `CDC / Outbox Polling` к `Notification Service` → `Notification Adapter` → внешний `Сервис уведомлений`. Опирается на [ADR-003](../docs/architecture/adr/adr-003-modular-monolith.md) инв. 4                                                                                                                                                          |
| 6   | Чистка плановых рассинхронов                                                                  | Выполнена до старта Фазы 4 (2026-05-02): мастер-план + регламент + журнал трассировки приведены в соответствие, в подпланах Фаз 1 и 2 добавлены постскриптумы                                                                                                                                                                                                                                                                                                                           |
| 7   | Webhook + polling fallback ([INT-014](../docs/specs/integration/integration-requirements.md)) | Webhook как основной канал на основной диаграмме; `opt polling fallback (после таймаута)` — отдельный опциональный блок с `cs->>edo: GET /documents/{id}/status`. Без раздувающего `alt webhook / polling`                                                                                                                                                                                                                                                                              |
| 8   | Пауза подписи (часы–дни)                                                                      | `Note over cs,edo: Ожидание подписи (до 7 календарных дней)` + `...` (графический разрыв Mermaid). Применяется дважды — для ожидания подписи клиента и ожидания финальной подписи Владельца                                                                                                                                                                                                                                                                                             |
| 9   | Просрочки 10б / 12б                                                                           | Два отдельных `break`-блока — по этапам маршрута (клиент / Владелец), симметрично 10а / 12а. Объединение в один блок не делаем — это семантически разные расширения по нумерации UC-8.2                                                                                                                                                                                                                                                                                                 |
| 10  | Стиль стрелок                                                                                 | Гибрид как в UC-12.1: русские названия participants + латинские эндпойнты в стрелках (`POST /api/contracts`, `webhook: document.signed.by.client`, `INSERT outbox_events (...)`) + русский текст в `Note over`. REST-эндпойнты будут консистентны с проектируемым в Фазе 5 контрактом                                                                                                                                                                                                   |
| 11  | Имена participants                                                                            | Без эмодзи и без P-номеров (`P14` / `P3` и т.д.). Display names: `PWA Клиента ЮЛ`, `API Gateway`, `Сервис Договоров`, `Client Profile`, `БД`, `Notification Service`, `Notification Adapter`, `ЭДО`, `Сервис уведомлений`. Aliases — короткие латинские (`pwa`, `gw`, `cs`, `cps`, `db`, `ns`, `na`, `edo`, `nsvc`)                                                                                                                                                                     |
| 12  | Уведомления INT-015                                                                           | Все 4 события (отправка / заключение / отклонение / просрочка). В стрелке — только тип события + получатель (например, `→ ns: ДоговорОтправленНаПодпись (получатель=клиент)`), без полного списка параметров. Полный набор атрибутов уже в [INT-015](../docs/specs/integration/integration-requirements.md)                                                                                                                                                                             |
| 13  | Идемпотентность повторов в ЭДО (INT-014)                                                      | `Note over cs,edo: Повторы идемпотентны по внутреннийИдДоговора (INT-014)` — одной заметкой, без отдельного `opt retry` блока. Бизнес-идемпотентность UC-8.2 (расширение `3а`, поиск существующего договора по бизнес-ключу) — это другой уровень и показывается отдельно как `alt` перед созданием                                                                                                                                                                                     |
| 14  | Структурная разбивка                                                                          | (a) `alt 3а. Существует договор по бизнес-ключу` в начале (после шагов 1–3 UC); (b) основной поток создание→формирование→передача→прием подтверждения; (c) `break 7а` между 7 и 8; (d) Note + `...` ожидания клиента; (e) `opt polling fallback` после интервала; (f) ветви по событиям ЭДО — основной поток / `break 10а` / `break 10б`; (g) аналогично для финальной подписи: Note + `...` + `opt polling` + основной / `break 12а` / `break 12б`; (h) outbox для каждого уведомления |
| 15  | Расположение `.puml`-исходника                                                                | Оба варианта: (1) inline блок ` ```plantuml ` в самом `sequence-uc-8-2-create-contract.md` (рядом с Mermaid); (2) отдельный файл [assets/sequence-uc-8-2-create-contract.puml](../docs/architecture/integration/assets/) для удобства рендера в IDE и PlantUML CLI                                                                                                                                                                                                                      |
| 16  | PNG-экспорт PlantUML                                                                          | Да: [assets/sequence-uc-8-2-create-contract.png](../docs/architecture/integration/assets/) рядом с `.puml`. Симметрично UC-10.2 (там PNG как источник). Без PNG ревьюер с inline-блоком увидит сырой код PlantUML, не картинку                                                                                                                                                                                                                                                          |
| 17  | Проверка `.puml`-синтаксиса в CI                                                              | Нет. Ручной рендер через [plantuml.com/plantuml](https://plantuml.com/plantuml) (web-encoder) или локальный CLI. PlantUML CLI требует Java + plantuml.jar — лишняя инфраструктура для одного учебного артефакта. Аналог `lint:mermaid` в проекте для PlantUML добавлять не будем (отдельный chore-task за пределами этого подплана)                                                                                                                                                     |
| 18  | Содержание PlantUML vs Mermaid                                                                | Точная копия 1:1: те же participants, стрелки, расширения, Note over, opt / alt / break-блоки. Разные диаграммы будут путать читателя; цель учебная — показать обе нотации на одном сценарии                                                                                                                                                                                                                                                                                            |

## Цель

Один файл-артефакт: [docs/architecture/integration/sequence-uc-8-2-create-contract.md](../docs/architecture/integration/sequence-uc-8-2-create-contract.md).

После Фазы 4:

- В файле .md две диаграммы — Mermaid (inline, рендерится в GitHub) и PlantUML (inline блок ` ```plantuml `, плюс ссылка на PNG-экспорт).
- В [assets/](../docs/architecture/integration/assets/) — `sequence-uc-8-2-create-contract.puml` (исходник) и `sequence-uc-8-2-create-contract.png` (рендер).
- Структура диаграммы покрывает основной поток UC-8.2 (~18 шагов) и 6 расширений (`3а`, `7а`, `10а`, `10б`, `12а`, `12б`).
- Outbox-механика для уведомлений отрисована явно по образцу UC-12.1.
- Webhook + `opt polling fallback` показывает асинхронный транспорт INT-014.
- Note over + `...` фиксирует SLA 7 календарных дней на каждый этап маршрута.
- В разделе «Связанные документы» — ссылки на UC-8.2, INT-014/015, регламент Блок 3, [DFD L1](../docs/artifacts/dfd-l1.md) (источник F03/F04), [ADR-003](../docs/architecture/adr/adr-003-modular-monolith.md) (outbox), UC-12.1 Sequence (стилевой образец).

## Структура артефакта

Файл .md по образцу [sequence-uc-12-1-pass-auto-identification-entry.md](../docs/architecture/integration/sequence-uc-12-1-pass-auto-identification-entry.md):

1. **Заголовок** `# Sequence Diagram — UC-8.2 Создать договор с ЮЛ через ЭДО`.
2. **Вводный абзац** — что визуализирует диаграмма, опора на UC-8.2 / INT-014 / INT-015 / регламент Блок 3.
3. **Раздел «Диаграмма Mermaid»** — inline блок ` ```mermaid ` с `sequenceDiagram`.
4. **Раздел «Диаграмма PlantUML»**:
   - Краткий абзац-ссылка: ссылка на PNG-экспорт + ссылка на `.puml`-исходник.
   - Embed PNG: `![UC-8.2 PlantUML](assets/sequence-uc-8-2-create-contract.png)`.
   - Inline блок ` ```plantuml ` — копия 1:1 содержимого `.puml`-исходника.
5. **Раздел «Связанные документы»** — стандартный по проекту.

### Список участников (имена и aliases)

| alias  | display name         | роль / источник                                                                                                                                                              |
| ------ | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pwa`  | PWA Клиента ЮЛ       | Инициатор сценария (триггер UC-8.2)                                                                                                                                          |
| `gw`   | API Gateway          | Контейнер C4 L2 — JWT, rate limit, маршрутизация                                                                                                                             |
| `cs`   | Сервис Договоров     | Доменный сервис (P14) — владелец агрегата `contracts`. Терминология синхронизирована с C4 / DFD L1 / Фазой 3                                                                 |
| `cps`  | Client Profile       | Доменный сервис (P3) — read-источник реквизитов ЮЛ через view (симметрично ADR-005)                                                                                          |
| `db`   | БД                   | Единая PostgreSQL ([ADR-003](../docs/architecture/adr/adr-003-modular-monolith.md) инв. 5). Используется для INSERT-ов в `contracts`, `outbox_events` и SELECT-ов через view |
| `ns`   | Notification Service | Доменный сервис (P9) — формирование уведомлений, поллит outbox                                                                                                               |
| `na`   | Notification Adapter | Адаптер (P20) — внешний транспорт уведомлений                                                                                                                                |
| `edo`  | ЭДО                  | Внешняя система E12 — оператор ЭДО (СБИС / Диадок). Передача документа, маршрут согласования, события статусов                                                               |
| `nsvc` | Сервис уведомлений   | Внешняя система E8 — SMS / push / email                                                                                                                                      |

### Состав основного потока (по UC-8.2 шагам 1–18)

Mermaid и PlantUML диаграммы воспроизводят последовательность шагов UC-8.2 буквально (не по «модельной семантике», а по нумерации UC):

- **Шаги 1–3** UC: PWA → API Gateway → `cs` (`POST /api/contracts` с параметрами формы); валидация / расчет стоимости — внутри `cs` (без отдельных стрелок к Pricing — это другой уровень детализации).
- **Шаг 5** (формирование документа): `cs->>cps: GET /clients/{id}/organization (через view)` для подстановки реквизитов ЮЛ → `cps-->>cs: реквизиты`.
- **Шаг 4** (создание `[договор]` в `статус=черновик`): `cs->>db: INSERT contracts (status=DRAFT, ...)`.
- **Шаги 6–7**: `cs->>edo: POST /documents (типДокумента, маршрут=Клиент→Владелец, внутреннийИдДоговора, реквизиты)` → `edo-->>cs: 200 OK (идДокументаЭДО, статусПриема)`. С `Note over cs,edo` про идемпотентность по `внутреннийИдДоговора`.
- **Шаги 8–9** (статус=на подписании клиентом + уведомление): `cs->>db: UPDATE contracts SET status=AWAITING_CLIENT_SIGNATURE` + `INSERT outbox_events (type=ДоговорОтправленНаПодпись, получатель=клиент)` (одной транзакцией) → асинхронно `db-->>ns: CDC / Outbox Polling` → `ns->>na: ...` → `na->>nsvc: send`.
- **Ожидание подписи клиента**: `Note over cs,edo` + `...`. Сразу за ним — `opt polling fallback (если webhook не доставлен в течение настраиваемого интервала)`: `cs->>edo: GET /documents/{id}/status` → `edo-->>cs: статус`.
- **Шаги 10–11**: основной случай — `edo->>cs: webhook: document.signed.by.client (идДокументаЭДО, моментПодписи, идентификаторПодписанта)` → `cs->>db: UPDATE contracts SET status=AWAITING_OWNER_SIGNATURE, client_signed_at=...`.
- **Ожидание финальной подписи**: Note + `...` + `opt polling fallback`.
- **Шаги 12–14**: `edo->>cs: webhook: document.fully.signed (идДокументаЭДО, содержимое, моментФинальнойПодписи)` → `cs->>db: file storage save (ссылка)` → `cs->>db: UPDATE contracts SET status=ACTIVE, document_file_ref=..., owner_signed_at=...`.
- **Шаг 15** (бронирования): `cs->>db: INSERT bookings (...)` для всех ПМ из формы (без раскрытия Booking Service на этом уровне; формулировка в Note).
- **Шаги 16–17** (два уведомления): `cs->>db: INSERT outbox_events (type=ДоговорЗаключен, получатель=клиент)` + `INSERT outbox_events (type=ДоговорЗаключен, получатель=Управляющий)` → асинхронно две доставки через ns / na / nsvc.

### Состав расширений

| Расширение                                 | Блок Mermaid                     | Где в потоке                                                           | Содержимое                                                                                                                                                                                                  |
| ------------------------------------------ | -------------------------------- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **3а.** Существует договор по бизнес-ключу | `alt 3а.` (else — новый договор) | После шагов 1–3, до шага 4 (создание `[договор]`)                      | `cs->>db: SELECT existing contract by business key (clientId, шаблонДоговораИд, пересекающиеся ПМ/период, статус не финальный)` → возврат сущ. договора с текущим статусом → `cs-->>pwa: 200 OK (existing)` |
| **7а.** Подтверждение приема не получено   | `break 7а.`                      | Между шагами 7 и 8                                                     | `cs` фиксирует таймаут / отсутствие `идДокументаЭДО`; `cs->>db: INSERT outbox_events (type=ОшибкаОтправки, получатель=клиент)`; `cs-->>pwa: 502 ЭДО недоступно`. Договор остается в `DRAFT`                 |
| **10а.** Отказ клиента                     | `break 10а.`                     | После Note ожидания клиента, как одна из веток webhook                 | `edo->>cs: webhook: document.rejected (сторонаОтказа=клиент, причинаОтказа)` → `cs->>db: UPDATE status=REJECTED_BY_CLIENT` + `INSERT outbox_events (type=ДоговорОтклонен, получатель=клиент)`               |
| **10б.** Просрочка клиента                 | `break 10б.`                     | После Note ожидания клиента, отдельная ветка по таймауту SLA           | `edo->>cs: webhook: document.expired (сторонаПросрочки=клиент)` → `cs->>db: UPDATE status=EXPIRED` + `INSERT outbox_events (type=ДоговорПросрочен, получатель=клиент)`                                      |
| **12а.** Отказ Владельца                   | `break 12а.`                     | После Note ожидания финальной подписи, как одна из веток webhook       | Симметрично 10а: `сторонаОтказа=Владелец`, статус `REJECTED_BY_OWNER`                                                                                                                                       |
| **12б.** Просрочка Владельца               | `break 12б.`                     | После Note ожидания финальной подписи, отдельная ветка по таймауту SLA | Симметрично 10б: `сторонаПросрочки=Владелец`, статус `EXPIRED`                                                                                                                                              |

## Содержательные правила (общие для обеих нотаций)

- **Имена participants** — синхронизированы с C4 L3 / DFD L1, без P-номеров и эмодзи. `Сервис Договоров` (а не «Договор» из INT-\* или «Contract Service» из C4).
- **Стрелки** — гибрид: латинские эндпойнты (`POST /api/contracts`, `GET /clients/{id}/organization`, `GET /documents/{id}/status`, `webhook: document.signed.by.client`, `INSERT outbox_events (...)`, `UPDATE contracts SET ...`); русский текст для семантических пометок в `Note over`.
- **Стиль блоков** — по [UC-12.1](../docs/architecture/integration/sequence-uc-12-1-pass-auto-identification-entry.md): `rect rgb(255, 235, 235)` + `break` для terminal failure (расширения 7а, 10а, 10б, 12а, 12б), `alt` для разветвлений с не-терминальным else (3а), `opt` для опциональных веток (polling fallback).
- **Outbox-механика** — `cs->>db: INSERT outbox_events (aggregate=CONTRACT, type=..., получатель=...)`. Асинхронная доставка показана пунктирной стрелкой `db-->>ns: CDC / Outbox Polling (асинхронно)`, далее `ns->>na: ...` и `na->>nsvc: send (...)`. Транзакционная семантика — `Note over cs,db: Транзакционный блок` (UPDATE + INSERT outbox в одной транзакции).
- **SLA 7 календарных дней** — фиксируется в `Note over cs,edo: Ожидание подписи клиента (до 7 календарных дней — INT-014 SLA)`. Не дублируем 7-дневный лимит в стрелках, не зашиваем в `idle`-таймер.
- **Идемпотентность повторов в ЭДО** — `Note over cs,edo: Повторы отправки идемпотентны по внутреннийИдДоговора (INT-014)` рядом с шагом 6.
- **Уведомления INT-015** — в стрелке только тип события + получатель: `→ ns: ДоговорЗаключен (получатель=клиент)`, `→ ns: ДоговорЗаключен (получатель=Управляющий)`, `→ ns: ДоговорОтклонен (получатель=клиент)`, `→ ns: ДоговорПросрочен (получатель=клиент)`. Полный набор атрибутов — в [INT-015](../docs/specs/integration/integration-requirements.md). Владелец не уведомляется платформой (это делает ЭДО его кабинетом) — отдельно в Note не повторяем (это уровень UC, не Sequence).
- **Текст без буквы «ё»**: «расширение», «удален», «снято», «получает», «передает» и т.д. Проверяется перед коммитом.

## Scope

> Часть исходных пунктов scope пересмотрена по ходу работы (Mermaid, inline-блок PlantUML, `lint:mermaid`) — фактический результат и обоснования в разделе [## Итог](#итог).

**Входит:**

- Один MD-файл: [sequence-uc-8-2-create-contract.md](../docs/architecture/integration/sequence-uc-8-2-create-contract.md) — embed PNG, ссылка на `.puml`-исходник, разделы «Участники», «Соответствие требованиям», «Связанные документы». ~~Inline-блок Mermaid + inline-блок PlantUML~~ — пересмотрено, см. [## Итог](#итог).
- Один PlantUML-исходник: [assets/sequence-uc-8-2-create-contract.puml](../docs/architecture/integration/assets/).
- Один PNG-экспорт PlantUML-диаграммы: [assets/sequence-uc-8-2-create-contract.png](../docs/architecture/integration/assets/).
- Обновление мастер-плана: статус Фазы 4 → `[x]`, ссылка на этот подплан в раздел «Подпланы», запись в `## Итог`.
- Обновление индекса [docs/architecture/integration/readme.md](../docs/architecture/integration/readme.md), если в нем перечислены sequence-артефакты.
- Прогон линтеров: `npm run lint:md`, `npm run lint:md-links`, `npm run lint:file-names`, `npm run check:plans`. ~~`npm run lint:mermaid`~~ — не применим (Mermaid-нотация не использована).

**Не входит:**

- Правка [UC-8.2](../docs/artifacts/use-case/uc-8-2-create-contract-legal-entity.md) — он уже зафиксирован Фазой 1.
- Правка [INT-014 / INT-015](../docs/specs/integration/integration-requirements.md) — они зафиксированы Фазой 2 (после `CHG-20260502-005`).
- Правка [is-interaction-regulation.md](../docs/architecture/integration/is-interaction-regulation.md) — Блок 3 закрыт Фазой 3 (наполнение Протокола / Полезной нагрузки — это Фаза 9).
- JSON-пример + JSON Schema — Фаза 5.
- XML + XSD — Фаза 6.
- Postman-коллекция — Фаза 7.
- OpenAPI yaml — Фаза 8.
- Запись `CHG-*` в [traceability-matrix-log.md](../docs/process/traceability-matrix-log.md) — мастер-план собирает одну общую запись в Фазе 10 на весь набор 9 артефактов.
- Добавление `lint:plantuml` в `npm run ci:check` — отдельный chore-task, не привязан к UC-8.2.
- ADR на PlantUML как новую нотацию — обсуждается отдельно, если PlantUML станет принятой нотацией для проекта (сейчас это разовый учебный артефакт).

## Definition of Done для Фазы 4

> Часть пунктов пересмотрена по ходу работы (Mermaid-нотация, 9 participants, inline-блок PlantUML, `lint:mermaid`). Историческая формулировка сохранена ниже как `~~strikethrough~~`; фактический результат и мотивация — в разделе [## Итог](#итог).

- [x] Файл [docs/architecture/integration/sequence-uc-8-2-create-contract.md](../docs/architecture/integration/sequence-uc-8-2-create-contract.md) создан (структура отличается от исходного раздела «Структура артефакта» — фактически: embed PNG + «Участники» + «Соответствие требованиям» + «Связанные документы», без inline-блоков).
- [x] PlantUML-диаграмма содержит:
  - 4 participants («Клиент ЮЛ», «Управляющий», «Система», «ЭДО»). ~~9 participants по таблице из секции «Список участников»~~ — упрощено, см. [## Итог](#итог).
  - Основной поток UC-8.2 (17 шагов).
  - 6 расширений (`3а`, `7а`, `10а`, `10б`, `12а`, `12б`) — `break` для `3а` и `7а`, `alt`/`else`/`break` для `10а/10б` и `12а/12б`.
  - Паузы `...Ожидание подписания...` (дважды, для клиента и Владельца) — фиксируют SLA 7 календарных дней.
  - Уведомления Клиенту и Управляющему — асинхронными стрелками от «Системы».
- [x] ~~PlantUML inline-блок в .md~~ — не добавлен (по решению пользователя — только embed PNG + ссылка на `.puml`-исходник, по образцу UC-10.2). Отдельный файл [assets/sequence-uc-8-2-create-contract.puml](../docs/architecture/integration/assets/) — есть.
- [x] PNG-экспорт PlantUML отрендерен через [plantuml.com/plantuml](https://plantuml.com/plantuml) и сохранен в [assets/sequence-uc-8-2-create-contract.png](../docs/architecture/integration/assets/). PNG читаем (текст не обрезается, структура ясна).
- [x] Стиль participants — без эмодзи, без P-номеров. ~~Aliases — короткие латинские (`pwa`, `gw`, `cs`, `cps`, `db`, `ns`, `na`, `edo`, `nsvc`)~~ — неактуально (4 participants вместо 9).
- [x] Стрелки — sync (`->`/`-->`) для запрос/ответ, async (`->>`) для webhook и уведомлений. ~~Гибрид с латинскими эндпойнтами (`POST /api/contracts`, `INSERT outbox_events`, `UPDATE contracts SET ...`)~~ — пересмотрено, эндпойнты и DB-операции свернуты внутрь «Системы»; конкретные REST-пути — в Фазе 5 (JSON+Schema).
- [x] Раздел «Связанные документы» содержит ссылки: UC-8.2, INT-014/015, регламент Блок 3, DFD L1 (источник F03/F04), ADR-003 (outbox), UC-12.1 Sequence (стилевой ориентир), родительский план UC-8.2.
- [x] [docs/architecture/integration/readme.md](../docs/architecture/integration/readme.md) обновлен — добавлена строка в раздел «Текущие материалы».
- [x] Мастер-план [2026-05-02-uc-8-2-edo-integration.md](2026-05-02-uc-8-2-edo-integration.md) обновлен: статус Фазы 4 → `[x]` со ссылкой на этот подплан, добавлен блок «Фаза 4 завершена 2026-05-28» в `## Итог`.
- [x] `npm run lint:md` — зеленый.
- [x] `npm run lint:md-links` — зеленый.
- [x] `npm run lint:file-names` — зеленый.
- [x] ~~`npm run lint:mermaid`~~ — не применим (Mermaid-блока в артефакте нет).
- [x] `npm run check:plans` — зеленый.
- [x] Текст без буквы «ё» (проверка `grep -nE 'ё|Ё' <files>` — пусто).
- [x] Запись `CHG-*` в [traceability-matrix-log.md](../docs/process/traceability-matrix-log.md) — **не делается** в этом подплане (одна общая запись в Фазе 10 мастер-плана).

## Фазы и статус

- [x] Шаг 4.1. Прочитать [UC-8.2](../docs/artifacts/use-case/uc-8-2-create-contract-legal-entity.md), [INT-014 / INT-015](../docs/specs/integration/integration-requirements.md), [Блок 3 регламента](../docs/architecture/integration/is-interaction-regulation.md), образец [UC-12.1 Sequence](../docs/architecture/integration/sequence-uc-12-1-pass-auto-identification-entry.md). Зафиксировать порядок шагов, имена статусов из расширенного `contract_status_enum`, точные названия событий ЭДО.
- [x] Шаг 4.2. Создать файл-скелет [sequence-uc-8-2-create-contract.md](../docs/architecture/integration/sequence-uc-8-2-create-contract.md) с заголовком, вводным абзацем, плейсхолдерами под Mermaid / PlantUML / PNG / Связанные документы.
- [x] Шаг 4.3. Написать Mermaid `sequenceDiagram` (полный): 9 participants, основной поток (~18 шагов), 6 расширений, два Note + `...`, два `opt polling fallback`, outbox-механика. Прогнать `npm run lint:mermaid` локально на этом блоке, поправить замечания.
- [x] Шаг 4.4. Скопировать диаграмму в PlantUML-нотацию (`@startuml` / `@enduml`, тот же набор участников, эквиваленты `alt` / `opt` / `break` / `note over`). Сохранить отдельный файл [assets/sequence-uc-8-2-create-contract.puml](../docs/architecture/integration/assets/) и вставить тот же текст inline в .md как блок ` ```plantuml `.
- [x] Шаг 4.5. Отрендерить PlantUML в PNG через [plantuml.com/plantuml](https://plantuml.com/plantuml) (или локальный CLI, если установлен Java + plantuml.jar). Сохранить как [assets/sequence-uc-8-2-create-contract.png](../docs/architecture/integration/assets/). Проверить PNG визуально: участники видны, текст не обрезан, блоки `alt` / `opt` / `break` различимы, расширения подписаны.
- [x] Шаг 4.6. Заполнить раздел «Связанные документы» (UC-8.2, INT-014/015, регламент Блок 3, DFD L1, ADR-003, UC-12.1 Sequence, родительский план).
- [x] Шаг 4.7. Проверить [docs/architecture/integration/readme.md](../docs/architecture/integration/readme.md): если в нем перечислены sequence-артефакты — добавить ссылку на новый файл.
- [x] Шаг 4.8. Прогнать `npm run lint:md`, `npm run lint:md-links`, `npm run lint:file-names`, `npm run lint:mermaid`, `npm run check:plans`. Поправить замечания. Проверить отсутствие буквы «ё» через `grep -nE 'ё|Ё'` по всем правленым файлам.
- [x] Шаг 4.9. Актуализировать мастер-план [2026-05-02-uc-8-2-edo-integration.md](2026-05-02-uc-8-2-edo-integration.md): переключить Фазу 4 на `[x]` с текстом «создан sequence-uc-8-2-create-contract.md (Mermaid + PlantUML + PNG-экспорт), 6 расширений, выбран и зафиксирован образец UC-12.1»; в разделе «Подпланы» заменить заглушку `2026-MM-DD-uc-8-2-sequence.md` на ссылку на этот файл; добавить блок «Фаза 4 завершена YYYY-MM-DD» в `## Итог` с перечнем артефактов и ключевыми решениями (если по ходу работы что-то уточнялось от исходного плана).

## Ручные действия пользователя после Фазы 4

Вне scope этого подплана и репо — пользователь делает руками:

- (Опционально) Добавить в buildin.ai ссылку / превью на новый Sequence в соответствующей странице UC-8.2 (синхронизация репо ↔ buildin), если в проекте принят воркфлоу синхронизации Sequence-артефактов (для UC-12.1 это не делалось — поведение для UC-8.2 — на усмотрение автора).
- Запуск рендера PlantUML, если у пользователя установлен локальный CLI: `plantuml docs/architecture/integration/assets/sequence-uc-8-2-create-contract.puml`. Если CLI нет — копипаст содержимого `.puml` в [plantuml.com/plantuml](https://plantuml.com/plantuml), скачивание PNG, сохранение в `assets/`.

## Итог

Подплан создан 2026-05-02, фаза завершена 2026-05-28.

**Артефакты созданы:**

- [docs/architecture/integration/sequence-uc-8-2-create-contract.md](../docs/architecture/integration/sequence-uc-8-2-create-contract.md) — основной артефакт (embed PNG + раздел «Связанные документы» + раздел «Участники» + раздел «Соответствие требованиям»).
- [docs/architecture/integration/assets/sequence-uc-8-2-create-contract.puml](../docs/architecture/integration/assets/sequence-uc-8-2-create-contract.puml) — PlantUML-исходник, ~70 строк.
- [docs/architecture/integration/assets/sequence-uc-8-2-create-contract.png](../docs/architecture/integration/assets/sequence-uc-8-2-create-contract.png) — PNG-экспорт через [plantuml.com/plantuml](https://plantuml.com/plantuml).
- [docs/architecture/integration/readme.md](../docs/architecture/integration/readme.md) — добавлена строка в раздел «Текущие материалы».

**Фактический результат vs план:**

- ✅ Основной поток (17 шагов) + 6 расширений (`3а`, `7а`, `10а`, `10б`, `12а`, `12б`) — покрыты.
- ✅ SLA 7 календарных дней показан паузами `...Ожидание подписания...` на двух этапах.
- ✅ Synchronous / asynchronous edges различимы: sync — `->`/`-->`, async (webhook, уведомления) — `->>`.
- ❌ Mermaid-нотация **не добавлена** (по решению пользователя — одна нотация PlantUML, по образцу UC-10.2). Решение #18 подплана не выполнено сознательно. Лимит `lint:mermaid` для этого артефакта не применим.
- ❌ Outbox-механика **не отображена явно** на диаграмме (свернута в «Систему»). Решение #5 подплана пересмотрено сознательно — детали уровня внутренних модулей переехали в Фазы 5/9.
- ❌ Polling fallback **не показан** на диаграмме (упрощение для UC-уровня). Решение #7 подплана пересмотрено — обоснование оставлено в `INT-014`.
- ❌ Participants — 4 вместо 9 (упрощение). Решение #11 подплана пересмотрено: вместо 9 participants без эмодзи использовано 4 («Клиент ЮЛ», «Управляющий», «Система», «ЭДО»). Внутренние модули свернуты, Сервис уведомлений скрыт за асинхронными стрелками.

**Что пошло иначе, чем в подплане:**

- **Расположение `break 7а`** — между шагами 6 и 7 (а не между 7 и 8, как в решении #14). Семантически точнее: 7а — это альтернатива получению подтверждения, а не следствие после него.
- **Структура 10а/10б и 12а/12б через `alt`/`else`/`break`** — не упоминалась в подплане в таком виде. Подплан фиксировал `break 10а / 10б` отдельными блоками, но фактически три альтернативы одного webhook'а потребовали `alt` (один из трех типов: signed / rejected / expired). Внутри failure-веток — вложенный `break` для явного UML-маркера «терминал».
- **`break 3а` вместо `alt 3а / else договор новый`** — изначально подплан фиксировал `alt`. Поменяли на `break` после обсуждения: `break` не заставляет вложить шаги 4–17 внутрь `else`-ветки, диаграмма получается чище.

**Шаги подплана:**

- [x] 4.1. Контекст прочитан (UC-8.2, INT-014/015, регламент Блок 3, UC-12.1 Sequence).
- [x] 4.2. Файл-скелет создан.
- [x] 4.3. PlantUML вместо Mermaid (изначально предполагался Mermaid; пользователь работал в PlantUML с самого начала). Линтер `lint:mermaid` для этого артефакта не применим.
- [x] 4.4. PlantUML-исходник сохранен в `assets/sequence-uc-8-2-create-contract.puml`. Inline-блок в `.md` не добавлен (по решению пользователя — только embed PNG + ссылка на `.puml`-исходник, по образцу UC-10.2).
- [x] 4.5. PNG отрендерен пользователем.
- [x] 4.6. Раздел «Связанные документы» заполнен.
- [x] 4.7. [readme.md](../docs/architecture/integration/readme.md) обновлен.
- [x] 4.8. Линтеры прогнаны (см. ретро для деталей). Буква «ё» — отсутствует.
- [x] 4.9. Мастер-план актуализирован (статус Фазы 4 → `[x]`, блок «Фаза 4 завершена 2026-05-28» в `## Итог`, обновлен раздел «Подпланы»).

**Открытые вопросы на последующие фазы:**

- Эскалация Управляющему при просрочке парковкой (расширение 12б) — на диаграмме нет (выбрана симметрия с 10а/10б). Если бизнес-сценарий требует, можно вернуть на этапе JSON-контракта (Фаза 5) или регламента v2 (Фаза 9).
- Polling fallback `INT-014` — на диаграмме не показан, требование `INT-014` остается единственным источником.
- Outbox-механика — раскроется в JSON-контракте (Фаза 5) и регламенте v2 (Фаза 9).
- Внутренние модули (P14, P9, P20, P3) — могут потребовать собственной диаграммы уровня контейнеров (вне scope UC-8.2 ДЗ).

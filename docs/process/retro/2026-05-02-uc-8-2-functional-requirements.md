# Ретро: Фаза 2 — ФТ интеграции UC-8.2

**Дата:** 2026-05-02
**Длительность:** ~30 мин
**Связанный план:** [2026-05-02-uc-8-2-edo-integration.md](../../../plans/2026-05-02-uc-8-2-edo-integration.md) (мастер-план UC-8.2, раздел «Фаза 2 завершена»; подплан Фазы 2 удален после завершения)
**Агент:** Claude Code

## 1. Задача

Написать 4 новых интеграционных требования (`INT-014/015/016/017`) для файла `docs/specs/integration/integration-requirements.md` в соответствии с UC-8.2: 2 внешних (ЭДО, Notification) и 2 внутренних (Contract↔EDO Adapter, Contract→Notification outbox). Каждое требование — атомарные подзаписи по типам взаимодействия.

## 2. Как решал

1. **Подготовка контекста**: прочитал подплан (8 шагов, структура), мастер-план (9 deliverables, архитектурные допущения), образцы существующих INT-\* (INT-001/003/004/010/011/012) для синхронизации стиля.
2. **Параллельный запуск 4 агентов**: запустил одновременно (в одном сообщении) 4 агентов с общей преамбулой и индивидуальными task-блоками. Каждый агент вернул готовый markdown-блок (без записи в файл).
3. **Trust but verify**: проверил результаты агентов на соответствие образцам:
   - INT-014: 6 подзаписей (✓), но была пара лишних `---` в конце — удалил.
   - INT-015: 4 подзаписи (✓), но одна ошибка в тексте про Владельца (написано «парковка» вместо «Владелец»), финальный абзац требовал переформатирования.
   - INT-016: 3 подзаписи (✓) с маркировкой впереди, но ошибка в второй подзаписи (также «парковка» вместо «Владелец»).
   - INT-017: 2 подзаписи (✓), грамматическая ошибка в глаголе (потребляет → потреблять).
4. **Собрал блоки оркестратором** (ролью я сам выступил): собрал 4 блока в правильном порядке с исправлениями, вставил в `docs/specs/integration/integration-requirements.md` после INT-013.
5. **Проверки**: прогнал линтеры (`npm run lint:md`, `npm run lint:md-links`, `npm run lint:file-names`), проверил отсутствие буквы «ё», прогнал `npm run ci:check`.
6. **Актуализация планов**: переключил Фазу 2 в мастер-плане на [x], добавил блок завершения в ## Итог. Аналогично в подплане: все 8 шагов на [x] + заполнил ## Итог.

## 3. Решил ли

**Да, полностью.**

- Все 4 новых INT-\* добавлены в `docs/specs/integration/integration-requirements.md`.
- Структура: INT-014 (6 подзаписей) + INT-015 (4 подзаписи) + INT-016 (3 подзаписи маркировкой) + INT-017 (2 подзаписи с маркировкой).
- Все требования соответствуют образцу `INT-001/003/004`: одна вводная фраза + bullet-список атрибутов.
- Стиль унифицирован: camelCase на русском для атрибутов, без латинских смесей, без markdown-ссылок внутри блоков.
- Проверки прошли: `npm run ci:check` ✓, буквы «ё» нет ✓, все линтеры зеленые ✓.

## 4. Эффективность

### Что сработало

- **Параллельный запуск 4 агентов**: избежал гонок за файл и паралелизировал написание. Хотя для 4 коротких блоков параллелизм дал маргинальный выигрыш (суммарный токен-расход примерно тот же), он значительно снизил риск конфликтов, если бы в паралльных чатах были другие правки.
- **Trust but verify + быстрые исправления**: агенты вернули ~90% качества (все основные структуры были верны), оставшиеся ошибки («парковка» вместо «Владелец», грамматика) исправлены за несколько Edit-вызовов.
- **Линтеры как сюжет**: все проверки на 100% прошли с первого раза (ни одной ошибки, ни одного предупреждения).

### Что можно улучшить

- **Детальнее инструктировал бы агентам для INT-015/016**: оба содержали ошибку про «Владелец» / «парковка», хотя в подплане это было явно указано. Агент B указал название сущности неправильно в третьей подзаписи, а агент C скопировал эту ошибку из своего разбора UC. Можно было бы добавить в промпт явный пример: «ВНИМАНИЕ: не пишите "парковка", пишите "Владелец"».
- **Оркестратор как отдельный шаг** был выполнен мной (агентом), но по замыслу задачи это должна была быть отдельная фаза, выполняемая пользователем или специальным агентом-оркестратором. В реальном workflow это снизило бы скорость, но повысило бы прозрачность сборки.

### Что повторить

- Параллельный запуск для множественных независимых артефактов (если их 3+) при условии, что потом очень быстро собираются вместе.
- `npm run ci:check` как финальная проверка перед актуализацией планов — это дало уверенность, что ничего не сломалось.

## 5. Как было и как стало

| Состояние                                            | До                                | После                                                   |
| ---------------------------------------------------- | --------------------------------- | ------------------------------------------------------- |
| `docs/specs/integration/integration-requirements.md` | INT-013 + разделитель (186 строк) | INT-013 + INT-014/015/016/017 + разделитель (395 строк) |
| `plans/2026-05-02-uc-8-2-edo-integration.md`         | Фаза 2 [ ], ## Итог только Фаза 1 | Фаза 2 [x], ## Итог с блоком завершения Фазы 2          |
| `plans/2026-05-02-uc-8-2-functional-requirements.md` | Шаги 2.1–2.8 [ ], пустой ## Итог  | Все шаги [x], заполненный ## Итог с перечнем INT-\*     |
| `npm run ci:check`                                   | (не проверялся)                   | ✓ Все линтеры, проверки, сборка — зеленый               |

## Функциональные требования: объект Чек

### Входные UC

- UC-10.6 «Получить чек»
- UC-10.7 «Просмотреть список оплат»
- UC-10.9 «Просмотреть задолженность в админке»
- UC-10.x «Оплата» (формирование и фискализация чеков)

### Кандидаты ФТ (иерархия Роль.Объект.Действие)

## КлиентФЛ

- Чек.ПросмотретьСписок

  Система должна предоставлять пользователю с ролью КлиентФЛ возможность просматривать список своих чеков с атрибутами:

  - идентификатор
  - `идПлатежа`
  - номерФискальный
  - датаВремяЧека
  - статусФискализации
  - сумма

  и фильтрацией по периоду и статусу.

## Охранник

- Чек.ПросмотретьСписок

  Система должна предоставлять пользователю с ролью Охранник возможность просматривать и фильтровать список всех чеков с атрибутами:

  - идентификатор
  - `идПлатежа`
  - номерФискальный
  - датаВремяЧека
  - статусФискализации
  - сумма

- Чек.Отчет

  Система должна предоставлять пользователю с ролью Охранник возможность формировать отчёт по чекам, отображая данные по группам `способОплаты`:

  - датаНачалаПериода
  - датаОкончанияПериода
  - статусФискализации
  - количествоЧековВГруппе
  - суммаЧековВГруппе

  Перечень данных внутри каждой группы:

  - идентификатор
  - `идПлатежа`
  - номерФискальный
  - датаВремяЧека
  - статусФискализации
  - сумма
  - `идПользователя`
  - `идСотрудника`

## Управляющий

- Чек.ПросмотретьСписок

  Система должна предоставлять пользователю с ролью Управляющий возможность просматривать и фильтровать список всех чеков с атрибутами:

  - идентификатор
  - `идПлатежа`
  - номерФискальный
  - датаВремяЧека
  - статусФискализации
  - сумма

- Чек.Отчет

  Система должна предоставлять пользователю с ролью Управляющий возможность формировать отчёт по чекам, отображая данные по группам `способОплаты`:

  - датаНачалаПериода
  - датаОкончанияПериода
  - статусФискализации
  - количествоЧековВГруппе
  - суммаЧековВГруппе

  Перечень данных внутри каждой группы:

  - идентификатор
  - `идПлатежа`
  - номерФискальный
  - датаВремяЧека
  - статусФискализации
  - сумма
  - `идПользователя`
  - `идСотрудника`

## Владелец

- Чек.ПросмотретьСписок

  Система должна предоставлять пользователю с ролью Владелец возможность просматривать и фильтровать список всех чеков с атрибутами:

  - идентификатор
  - `идПлатежа`
  - номерФискальный
  - датаВремяЧека
  - статусФискализации
  - сумма

- Чек.Отчет

  Система должна предоставлять пользователю с ролью Владелец возможность формировать отчёт по чекам, отображая данные по группам `способОплаты`:

  - датаНачалаПериода
  - датаОкончанияПериода
  - статусФискализации
  - количествоЧековВГруппе
  - суммаЧековВГруппе

  Перечень данных внутри каждой группы:

  - идентификатор
  - `идПлатежа`
  - номерФискальный
  - датаВремяЧека
  - статусФискализации
  - сумма
  - `идПользователя`
  - `идСотрудника`

## Системы

- Чек.Создать.СД

  Система должна при успешном завершении записи Платеж со статусом `успешен` автоматически:

  - создавать связанную запись Чек
  - заполнять `идПлатежа`
  - устанавливать `сумма`, равную сумме платежа
  - устанавливать `датаВремяЧека` текущим системным значением
  - задавать `статусФискализации` = `ожидание`

- Чек.Изменить.СД.статусФискализации

  Система должна для записей Чек со `статусФискализации` = `ожидание`:

  - формировать запрос к фискальному регистратору/ОФД с необходимыми реквизитами чека
  - по результатам присваивать атрибуту `номерФискальный` значение, полученное от внешней системы
  - при необходимости обновлять `датаВремяЧека`
  - изменять `статусФискализации` на значения из фиксированного набора

- Чек.ПовторноОтправить.СД

  Система должна для записей Чек со `статусФискализации` = `ошибка`:

  - обеспечивать возможность раз в сутки повторного формирования и отправки данных чека во внешнюю фискальную систему
  - по результатам повторной попытки обновлять:
    - номерФискальный
    - датаВремяЧека
    - статусФискализации

### Проверка полноты набора ФТ

- Основные потоки UC-10.6 и смежных сценариев оплаты покрыты:
  - просмотр списка чеков
  - отчётность по чекам
  - автоматическое создание чека
  - фискализация и повторная отправка
- Потенциально отсутствующие ФТ (рекомендации к расширению):
  - ФТ по просмотру детальной карточки чека
  - ФТ по скачиванию или отправке чека клиенту
  - ФТ по повторной выдаче чека по запросу клиента

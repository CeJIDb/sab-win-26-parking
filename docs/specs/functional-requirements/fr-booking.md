## Функциональные требования: объект Бронирование

### Входные UC

- UC-7.1 «Создать бронирование»
- UC-7.2 «Просмотреть бронирование»
- UC-7.3 «Изменить бронирование»
- UC-7.4 «Отменить бронирование»
- UC-7.5 «Просмотреть список бронирований»
- UC-7.6 «Создать бронирование в админке»
- UC-7.7 «Просмотреть бронирование в админке»
- UC-7.8 «Изменить бронирование в админке»
- UC-7.9 «Отменить бронирование в админке»
- UC-7.10 «Просмотреть список бронирований в адм»
- UC-8.x «Договор» (связь долгосрочных бронирований с договорами)
- UC-10.x «Оплата» (использование суммы к оплате по бронированию)
- UC-12.2 «Создать бронирование автоматически на въезде»
- UC-12.9 «Завершить бронирование автоматически на выезде»

### Кандидаты ФТ (иерархия Роль.Объект.Действие)

## КлиентФЛ

- Бронирование.Создать

  Система должна позволять пользователю с ролью КлиентФЛ создавать объект Бронирование со следующими атрибутами:

  - идентификатор
  - тсИд
  - секторИд
  - парковочноеМестоИд
  - договорИд
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - тарифИд
  - суммаКОплате

- Бронирование.Просмотреть

  Система должна позволять пользователю с ролью КлиентФЛ просматривать принадлежащий ему объект Бронирование со следующими атрибутами:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - `[Договор]:номерДоговора`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - `[Тариф]:наименование`
  - суммаКОплате

- Бронирование.Изменить

  Система должна позволять пользователю с ролью КлиентФЛ изменять принадлежащий ему объект Бронирование со следующими атрибутами:

  - секторИд
  - парковочноеМестоИд
  - датаНачала
  - датаОкончания
  - длительность

- Бронирование.Отменить

  Система должна позволять пользователю с ролью КлиентФЛ отменять принадлежащий ему объект Бронирование.

- Бронирование.ПросмотретьСписок

  Система должна позволять пользователю с ролью КлиентФЛ просматривать список принадлежащих ему объектов Бронирования с отображением для каждого бронирования следующих атрибутов:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - суммаКОплате

## КлиентЮЛ

- Бронирование.Создать

  Система должна позволять пользователю с ролью КлиентЮЛ создавать объект Бронирование со следующими атрибутами:

  - идентификатор
  - тсИд
  - секторИд
  - парковочноеМестоИд
  - договорИд
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - тарифИд
  - суммаКОплате

- Бронирование.Просмотреть

  Система должна позволять пользователю с ролью КлиентЮЛ просматривать принадлежащий ему объект Бронирование со следующими атрибутами:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - `[Договор]:номерДоговора`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - `[Тариф]:наименование`
  - суммаКОплате

- Бронирование.Изменить

  Система должна позволять пользователю с ролью КлиентЮЛ изменять принадлежащий ему объект Бронирование со следующими атрибутами:

  - секторИд
  - парковочноеМестоИд
  - датаНачала
  - датаОкончания
  - длительность

- Бронирование.Отменить

  Система должна позволять пользователю с ролью КлиентЮЛ отменять принадлежащий ему объект Бронирование.

- Бронирование.ПросмотретьСписок

  Система должна позволять пользователю с ролью КлиентЮЛ просматривать список принадлежащих ему объектов Бронирования с отображением для каждого бронирования следующих атрибутов:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - суммаКОплате

## Охранник

- Бронирование.Создать

  Система должна позволять пользователю с ролью Охранник создавать объект Бронирование от имени клиента со следующими атрибутами:

  - тсИд
  - секторИд
  - парковочноеМестоИд
  - договорИд
  - датаНачала
  - датаОкончания
  - тарифИд

- Бронирование.Создать.СД

  Система должна изменять Бронирование со следующими атрибутами:

  - идентификатор
  - длительность
  - тип
  - статус
  - суммаКОплате

- Бронирование.Создать.СД.идентификатор

  Система должна изменять `[Бронирование]:идентификатор` по следующему алгоритму:

  - `[Бронирование]:идентификатор` = max(`[Бронирование]:идентификатор`) + 1.

- Бронирование.Создать.СД.длительность

  Система должна изменять `[Бронирование]:длительность` по следующему алгоритму:

  - `[Бронирование]:длительность` = `[Бронирование]:датаОкончания` - `[Бронирование]:датаНачала`
  - пересчитать в минуты, часы, дни, месяцы в зависимости от получившейся длительности:
    - если длительность < 60 минут, показывать в минутах;
    - если длительность < 24 часов, показывать в часах и минутах;
    - если длительность < 30 суток, показывать в днях и часах;
    - если длительность >= 30 суток, показывать в месяцах и днях.

- Бронирование.Создать.СД.тип

  Система должна изменять `[Бронирование]:тип` по следующему алгоритму:

  - если `[Бронирование]:длительность` < 24 ч, то `[Бронирование]:тип` = «краткосрочное»
  - если `[Бронирование]:длительность` >= 24 ч и существует Договор, связанный с [Бронирование], то `[Бронирование]:тип` = «долгосрочное»

- Бронирование.Создать.СД.статус

  Система должна изменять `[Бронирование]:статус` по следующему алгоритму:

  - `[Бронирование]:статус` = «ожидает оплаты».

- Бронирование.Создать.СД.суммаКОплате

  Система должна изменять `[Бронирование]:суммаКОплате` по следующему алгоритму:

  - `[Бронирование]:суммаКОплате` рассчитывается по `[Бронирование]:тарифИд` и `[Бронирование]:длительность`
  - результат округляется до минимальной денежной единицы (например, до копеек).

- Бронирование.Просмотреть

  Система должна позволять пользователю с ролью Охранник просматривать объект Бронирование со следующими атрибутами:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - `[Договор]:номерДоговора`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - `[Тариф]:наименование`
  - суммаКОплате

- Бронирование.Изменить

  Система должна позволять пользователю с ролью Охранник изменять объект Бронирование со следующими атрибутами:

  - секторИд
  - парковочноеМестоИд
  - датаНачала
  - датаОкончания

- Бронирование.Изменить.СД

  Система должна изменять Бронирование со следующими атрибутами:

  - длительность
  - статус

- Бронирование.Изменить.СД.длительность

  Система должна изменять `[Бронирование]:длительность` по следующему алгоритму:

  - `[Бронирование]:длительность` = `[Бронирование]:датаОкончания` - `[Бронирование]:датаНачала`
  - пересчитать в минуты, часы, дни, месяцы в зависимости от получившейся длительности:
    - если длительность < 60 минут, показывать в минутах;
    - если длительность < 24 часов, показывать в часах и минутах;
    - если длительность < 30 суток, показывать в днях и часах;
    - если длительность >= 30 суток, показывать в месяцах и днях.

- Бронирование.Изменить.СД.статус

  Система должна изменять `[Бронирование]:статус` по следующему алгоритму:

  - если существует Платеж, связанный с [Бронирование], и `[Платеж]:статус` = «успешен», то `[Бронирование]:статус` = «подтверждено»
  - если текущее системное время >= `[Бронирование]:датаНачала` и `[Бронирование]:статус` = «подтверждено» и существует ПарковочнаяСессия, связанная с [Бронирование], то `[Бронирование]:статус` = «активно»
  - если текущее системное время >= `[Бронирование]:датаОкончания` и `[Бронирование]:статус` = «активно», то `[Бронирование]:статус` = «завершено»
  - если текущее системное время >= `[Бронирование]:датаОкончания` и `[Бронирование]:статус` = «подтверждено», то `[Бронирование]:статус` = «просрочено»

- Бронирование.Отменить

  Система должна позволять пользователю с ролью Охранник отменить Бронирование.

- Бронирование.Отменить.СД

  Система должна изменять Бронирование со следующими атрибутами:

  - статус = «отменено»

- Бронирование.ПросмотретьСписок

  Система должна позволять пользователю с ролью Охранник просматривать список объектов Бронирование с отображением для каждого бронирования следующих атрибутов:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - `[Договор]:номерДоговора`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - суммаКОплате

## Управляющий

- Бронирование.Создать

  Система должна позволять пользователю с ролью Управляющий создавать объект Бронирование от имени клиента со следующими атрибутами:

  - тсИд
  - секторИд
  - парковочноеМестоИд
  - договорИд
  - датаНачала
  - датаОкончания
  - тарифИд

- Бронирование.Создать.СД

  Система должна изменять Бронирование со следующими атрибутами:

  - идентификатор
  - длительность
  - тип
  - статус
  - суммаКОплате

- Бронирование.Создать.СД.идентификатор

  Система должна изменять `[Бронирование]:идентификатор` по следующему алгоритму:

  - `[Бронирование]:идентификатор` = max(`[Бронирование]:идентификатор`) + 1.

- Бронирование.Создать.СД.длительность

  Система должна изменять `[Бронирование]:длительность` по следующему алгоритму:

  - `[Бронирование]:длительность` = `[Бронирование]:датаОкончания` - `[Бронирование]:датаНачала`
  - пересчитать в минуты, часы, дни, месяцы в зависимости от получившейся длительности:
    - если длительность < 60 минут, показывать в минутах;
    - если длительность < 24 часов, показывать в часах и минутах;
    - если длительность < 30 суток, показывать в днях и часах;
    - если длительность >= 30 суток, показывать в месяцах и днях.

- Бронирование.Создать.СД.тип

  Система должна изменять `[Бронирование]:тип` по следующему алгоритму:

  - если `[Бронирование]:длительность` < 24 ч, то `[Бронирование]:тип` = «краткосрочное»
  - если `[Бронирование]:длительность` >= 24 ч и существует Договор, связанный с [Бронирование], то `[Бронирование]:тип` = «долгосрочное»

- Бронирование.Создать.СД.статус

  Система должна изменять `[Бронирование]:статус` по следующему алгоритму:

  - `[Бронирование]:статус` = «ожидает оплаты».

- Бронирование.Создать.СД.суммаКОплате

  Система должна изменять `[Бронирование]:суммаКОплате` по следующему алгоритму:

  - `[Бронирование]:суммаКОплате` рассчитывается по `[Бронирование]:тарифИд` и `[Бронирование]:длительность`
  - результат округляется до минимальной денежной единицы (например, до копеек).

- Бронирование.Просмотреть

  Система должна позволять пользователю с ролью Управляющий просматривать объект Бронирование со следующими атрибутами:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - `[Договор]:номерДоговора`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - `[Тариф]:наименование`
  - суммаКОплате

- Бронирование.Изменить

  Система должна позволять пользователю с ролью Управляющий изменять объект Бронирование со следующими атрибутами:

  - секторИд
  - парковочноеМестоИд
  - датаНачала
  - датаОкончания

- Бронирование.Изменить.СД

  Система должна изменять Бронирование со следующими атрибутами:

  - длительность
  - статус

- Бронирование.Изменить.СД.длительность

  Система должна изменять `[Бронирование]:длительность` по следующему алгоритму:

  - `[Бронирование]:длительность` = `[Бронирование]:датаОкончания` - `[Бронирование]:датаНачала`
  - пересчитать в минуты, часы, дни, месяцы в зависимости от получившейся длительности:
    - если длительность < 60 минут, показывать в минутах;
    - если длительность < 24 часов, показывать в часах и минутах;
    - если длительность < 30 суток, показывать в днях и часах;
    - если длительность >= 30 суток, показывать в месяцах и днях.

- Бронирование.Изменить.СД.статус

  Система должна изменять `[Бронирование]:статус` по следующему алгоритму:

  - если существует Платеж, связанный с [Бронирование], и `[Платеж]:статус` = «успешен», то `[Бронирование]:статус` = «подтверждено»
  - если текущее системное время >= `[Бронирование]:датаНачала` и `[Бронирование]:статус` = «подтверждено» и существует ПарковочнаяСессия, связанная с [Бронирование], то `[Бронирование]:статус` = «активно»
  - если текущее системное время >= `[Бронирование]:датаОкончания` и `[Бронирование]:статус` = «активно», то `[Бронирование]:статус` = «завершено»
  - если текущее системное время >= `[Бронирование]:датаОкончания` и `[Бронирование]:статус` = «подтверждено», то `[Бронирование]:статус` = «просрочено»

- Бронирование.Отменить

  Система должна позволять пользователю с ролью Управляющий отменить Бронирование.

- Бронирование.Отменить.СД

  Система должна изменять Бронирование со следующими атрибутами:

  - статус = «отменено»

- Бронирование.ПросмотретьСписок

  Система должна позволять пользователю с ролью Управляющий просматривать список объектов Бронирование с отображением для каждого бронирования следующих атрибутов:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - `[Договор]:номерДоговора`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - суммаКОплате

## Владелец

- Бронирование.Создать

  Система должна позволять пользователю с ролью Владелец создавать объект Бронирование от имени клиента со следующими атрибутами:

  - тсИд
  - секторИд
  - парковочноеМестоИд
  - договорИд
  - датаНачала
  - датаОкончания
  - тарифИд

- Бронирование.Создать.СД

  Система должна изменять Бронирование со следующими атрибутами:

  - идентификатор
  - длительность
  - тип
  - статус
  - суммаКОплате

- Бронирование.Создать.СД.идентификатор

  Система должна изменять `[Бронирование]:идентификатор` по следующему алгоритму:

  - `[Бронирование]:идентификатор` = max(`[Бронирование]:идентификатор`) + 1.

- Бронирование.Создать.СД.длительность

  Система должна изменять `[Бронирование]:длительность` по следующему алгоритму:

  - `[Бронирование]:длительность` = `[Бронирование]:датаОкончания` - `[Бронирование]:датаНачала`
  - пересчитать в минуты, часы, дни, месяцы в зависимости от получившейся длительности:
    - если длительность < 60 минут, показывать в минутах;
    - если длительность < 24 часов, показывать в часах и минутах;
    - если длительность < 30 суток, показывать в днях и часах;
    - если длительность >= 30 суток, показывать в месяцах и днях.

- Бронирование.Создать.СД.тип

  Система должна изменять `[Бронирование]:тип` по следующему алгоритму:

  - если `[Бронирование]:длительность` < 24 ч, то `[Бронирование]:тип` = «краткосрочное»
  - если `[Бронирование]:длительность` >= 24 ч и существует Договор, связанный с [Бронирование], то `[Бронирование]:тип` = «долгосрочное»

- Бронирование.Создать.СД.статус

  Система должна изменять `[Бронирование]:статус` по следующему алгоритму:

  - `[Бронирование]:статус` = «ожидает оплаты».

- Бронирование.Создать.СД.суммаКОплате

  Система должна изменять `[Бронирование]:суммаКОплате` по следующему алгоритму:

  - `[Бронирование]:суммаКОплате` рассчитывается по `[Бронирование]:тарифИд` и `[Бронирование]:длительность`
  - результат округляется до минимальной денежной единицы (например, до копеек).

- Бронирование.Просмотреть

  Система должна позволять пользователю с ролью Владелец просматривать объект Бронирование со следующими атрибутами:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - `[Договор]:номерДоговора`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - `[Тариф]:наименование`
  - суммаКОплате

- Бронирование.Изменить

  Система должна позволять пользователю с ролью Владелец изменять объект Бронирование со следующими атрибутами:

  - секторИд
  - парковочноеМестоИд
  - датаНачала
  - датаОкончания

- Бронирование.Изменить.СД

  Система должна изменять Бронирование со следующими атрибутами:

  - длительность
  - статус

- Бронирование.Изменить.СД.длительность

  Система должна изменять `[Бронирование]:длительность` по следующему алгоритму:

  - `[Бронирование]:длительность` = `[Бронирование]:датаОкончания` - `[Бронирование]:датаНачала`
  - пересчитать в минуты, часы, дни, месяцы в зависимости от получившейся длительности:
    - если длительность < 60 минут, показывать в минутах;
    - если длительность < 24 часов, показывать в часах и минутах;
    - если длительность < 30 суток, показывать в днях и часах;
    - если длительность >= 30 суток, показывать в месяцах и днях.

- Бронирование.Изменить.СД.статус

  Система должна изменять `[Бронирование]:статус` по следующему алгоритму:

  - если существует Платеж, связанный с [Бронирование], и `[Платеж]:статус` = «успешен», то `[Бронирование]:статус` = «подтверждено»
  - если текущее системное время >= `[Бронирование]:датаНачала` и `[Бронирование]:статус` = «подтверждено» и существует ПарковочнаяСессия, связанная с [Бронирование], то `[Бронирование]:статус` = «активно»
  - если текущее системное время >= `[Бронирование]:датаОкончания` и `[Бронирование]:статус` = «активно», то `[Бронирование]:статус` = «завершено»
  - если текущее системное время >= `[Бронирование]:датаОкончания` и `[Бронирование]:статус` = «подтверждено», то `[Бронирование]:статус` = «просрочено»

- Бронирование.Отменить

  Система должна позволять пользователю с ролью Владелец отменить Бронирование.

- Бронирование.Отменить.СД

  Система должна изменять Бронирование со следующими атрибутами:

  - статус = «отменено»

- Бронирование.ПросмотретьСписок

  Система должна позволять пользователю с ролью Владелец просматривать список объектов Бронирование с отображением для каждого бронирования следующих атрибутов:

  - номерБронирования
  - `[ТС]:госНомер`
  - `[Сектор]:наименование`
  - `[ПМ]:номерМеста`
  - `[Договор]:номерДоговора`
  - датаНачала
  - датаОкончания
  - длительность
  - тип
  - статус
  - суммаКОплате

## Системы

- Бронирование.Создать.СД

  Система должна создать объект Бронирование при автоматическом въезде ТС на основании принятого СобытиеСКУД со следующими атрибутами:

  - идентификатор
  - тсИд
  - секторИд
  - парковочноеМестоИд
  - датаНачала
  - тип
  - статус
  - тарифИд

- Бронирование.Создать.СД.идентификатор

  Система должна изменять `[Бронирование]:идентификатор` по следующему алгоритму:

  - `[Бронирование]:идентификатор` = max(`[Бронирование]:идентификатор`) + 1

- Бронирование.Создать.СД.тсИд

  Система должна изменять `[Бронирование]:тсИд` по следующему алгоритму:

  - `[Бронирование]:тсИд` = идентификатор ТС, найденного по `[СобытиеСКУД]:ГРЗ` в UC-12.1

- Бронирование.Создать.СД.секторИд

  Система должна изменять `[Бронирование]:секторИд` по следующему алгоритму:

  - система определяет по выбранному Тарифу и типу ТС множество допустимых Типов зон
  - система формирует множество Секторов, у которых Тип зоны входит в множество допустимых Типов зон и в которых есть хотя бы одно свободное ПМ
  - если для типа клиента или ТС определены приоритетные зоны, система ограничивает множество Секторов только приоритетными Секторами (если такие есть)
  - из оставшихся Секторов система выбирает Сектор, ближайший к КПП, по которому получено СобытиеСКУД (по `[СобытиеСКУД]:кппИд`); при равном расстоянии выбирается Сектор с наименьшей загрузкой (минимальной долей занятых ПМ)
  - `[Бронирование]:секторИд` = идентификатор выбранного Сектора

- Бронирование.Создать.СД.парковочноеМестоИд

  Система должна изменять `[Бронирование]:парковочноеМестоИд` по следующему алгоритму:

  - система формирует множество ПМ в Секторе с идентификатором `[Бронирование]:секторИд`, удовлетворяющих условиям:
    - ПМ свободно
  - из множества подходящих ПМ система выбирает одно ПМ по следующей стратегии:
    - выбирается ПМ с минимальным идентификатором внутри Сектора (наименьшее значение идентификатора ПМ среди подходящих ПМ)
  - `[Бронирование]:парковочноеМестоИд` = идентификатор выбранного ПМ

- Бронирование.Создать.СД.датаНачала

  Система должна изменять `[Бронирование]:датаНачала` по следующему алгоритму:

  - `[Бронирование]:датаНачала` = `[СобытиеСКУД]:датаВремяСобытия`

- Бронирование.Создать.СД.тип

  Система должна изменять `[Бронирование]:тип` по следующему алгоритму:

  - если Бронирование создаётся в UC-12.2 по результатам UC-12.1, то `[Бронирование]:тип` = «автоматическое»

- Бронирование.Создать.СД.статус

  Система должна изменять `[Бронирование]:статус` по следующему алгоритму:

  - при успешном создании Бронирования в момент въезда ТС `[Бронирование]:статус` = «активно»

- Бронирование.Создать.СД.тарифИд

  Система должна изменять `[Бронирование]:тарифИд` по следующему алгоритму:

  - система определяет тип ТС по объекту ТС, связанному с `[Бронирование]:тсИд`
  - система формирует множество краткосрочных Тарифов, доступных для данного типа ТС
  - для каждого Тарифа из множества система определяет, какие Типы зон ему соответствуют, и какие Секторы парковки относятся к этим Типам зон
  - из множества краткосрочных Тарифов система исключает Тарифы, для которых на текущей парковке нет ни одного Сектора с подходящим Типом зоны и хотя бы одним свободным ПМ
  - если после исключения осталось несколько Тарифов, система выбирает среди них Тариф с минимальной базовой стоимостью (по настройкам тарифа); при равной базовой стоимости выбирается Тариф с наименьшей продолжительностью минимального расчетного интервала
  - `[Бронирование]:тарифИд` = идентификатор выбранного краткосрочного Тарифа

- Бронирование.Изменить.СД

  Система должна изменить Бронирование при автоматическом завершении на выезде со следующими атрибутами:

  - датаОкончания
  - длительность
  - статус
  - суммаКОплате

- Бронирование.Изменить.СД.статус

  Система должна изменять `[Бронирование]:статус` по следующему алгоритму:

  - если тип Бронирования ∈ {«краткосрочное», «автоматическое»} и существует ПарковочнаяСессия, связанная с [Бронирование], такая, что `[ПарковочнаяСессия]:статус` = «завершена», то `[Бронирование]:статус` = «завершено»

- Бронирование.Изменить.СД.датаОкончания

  Система должна изменять `[Бронирование]:датаОкончания` по следующему алгоритму:

  - `[Бронирование]:датаОкончания` = `[СобытиеСКУД]:датаВремяСобытия`

- Бронирование.Изменить.СД.длительность

  Система должна изменять `[Бронирование]:длительность` по следующему алгоритму:

  - `[Бронирование]:длительность` = округлённая до минут разница между `[Бронирование]:датаОкончания` и `[Бронирование]:датаНачала`
  - если `[Бронирование]:датаОкончания` < `[Бронирование]:датаНачала`, система не должна изменять `[Бронирование]:длительность` и должна зафиксировать ошибку в журнале аудита
  - если `[Бронирование]:статус` ∈ {«подтверждено», «активно»}, то `[Бронирование]:длительность` = округлённая до минут разница между текущим системным временем и `[Бронирование]:датаНачала`
  - если текущаяДатаВремя < `[Бронирование]:датаНачала`, система не должна изменять `[Бронирование]:длительность` и должна зафиксировать ошибку в журнале аудита

- Бронирование.Изменить.СД.суммаКОплате

  Система должна изменять `[Бронирование]:суммаКОплате` по следующему алгоритму:

  - базовая сумма рассчитывается по Тарифу, типу Бронирования, датеНачала и длительности
  - для активного краткосрочного бронирования базовая сумма рассчитывается по краткосрочному Тарифу с идентификатором `[Бронирование]:тарифИд` и текущему значению `[Бронирование]:длительность` (пересчитанному в минимальные тарифные единицы в соответствии с настройками тарифа)
  - при наличии льготного тарифа или договора с особыми условиями применяется соответствующая скидка/надбавка
  - результат округляется до минимальной денежной единицы (например, до копеек) по правилам тарификации и сохраняется в `[Бронирование]:суммаКОплате`

### Проверка полноты набора ФТ

- Основные потоки UC-7.x, UC-8.x, UC-10.x и UC-12.2/12.9 покрыты:
  - создание, просмотр, изменение, отмена и списки бронирований для клиента и админки
  - автоматическое создание и завершение бронирований при въезде/выезде
  - расчёт суммы к оплате по бронированию
- Потенциально отсутствующие ФТ (рекомендации к расширению):
  - СД-ФТ по дополнительным состояниям статуса (например, «ожидает оплаты», «просрочено») в связке с UC-10.x и уведомлениями
  - ФТ по фильтрации и поиску бронирований в списках по типу, статусу, периоду и связанным сущностям (ТС, договор, клиент)
  - ФТ по массовым операциям с бронированиями в админке (изменение статуса группы бронирований при технических работах и т.п.)

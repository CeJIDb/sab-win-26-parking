# UC-8.2 Создать договор с ЮЛ (долгосрочная аренда)

## Оглавление

- [Метаданные](#метаданные)
- [Предусловия](#предусловия)
- [Триггер](#триггер)
- [Постусловие](#постусловие)
- [Основной поток](#основной-поток)
- [Расширение](#расширение)
- [Требования для UC-8.2 → интеграции](#требования-для-uc-82--интеграции)
- [Связанные документы](#связанные-документы)

## Метаданные

| Поле                 | Значение                                                           |
| -------------------- | ------------------------------------------------------------------ |
| **Автор**            | cejidb                                                             |
| **Название**         | Сценарий интеграции. Создать договор с ЮЛ (долгосрочная аренда)    |
| **Действующее лицо** | Клиент ЮЛ, Система, Владелец, ЭДО, Сервис уведомлений, Управляющий |

## Предусловия

- Клиент ЮЛ авторизован в ЛК.
- В профиле клиента заполнены данные организации (UC-2.13 «Добавить данные организации»).
- В профиле клиента заполнены банковские реквизиты организации.
- В профиле клиента есть хотя бы одно ТС.
- Клиент подключен к одному из операторов ЭДО, поддерживаемых платформой (или есть роуминг с тем оператором, где работает Владелец).
- У Владельца есть аккаунт и действующая КЭП в том же операторе ЭДО (или совместимом через роуминг) для подписания договоров со стороны платформы.
- Существует опубликованный шаблон договора долгосрочной аренды для ЮЛ.
- Доступны для бронирования ПМ в нужном секторе на нужный срок.

## Триггер

Клиент ЮЛ в ЛК переходит в раздел «Создать договор».

## Постусловие

- `[договор].статус` = `активен`.
- `[договор].файлДоговора` указывает на сохраненный финальный документ с двумя ЭП.
- `[договор].датаПодписанияКлиентом` и `[договор].датаПодписанияПарковкой` заполнены.
- На указанные клиентом ПМ созданы бронирования на весь срок договора.
- Клиент получил уведомление об успешном заключении договора и доступности договора в ЛК.
- Управляющий получил уведомление о новом подписанном договоре.

## Основной поток

1. Клиент в форме «Создать договор» указывает параметры аренды:
   - `датаНачала`;
   - `датаОкончания`;
   - тариф;
   - ТС;
   - ПМ.
2. Система рассчитывает стоимость по выбранному клиентом тарифу и отображает условия договора (сумма, период, ПМ, ТС).
3. Клиент подтверждает «Заключить договор» → «Да».
4. Система создает `[договор]` со следующими атрибутами:
   - `идентификатор` — автоген;
   - `клиентИд` — ссылка на клиента из сессии;
   - `шаблонДоговораИд` — ссылка на опубликованный шаблон долгосрочной аренды для ЮЛ;
   - `номерДоговора` — автоген по правилу нумерации;
   - `датаНачала`, `датаОкончания` — из формы;
   - `статус` = `черновик`;
   - `файлДоговора` = `null`.
5. Система формирует документ договора по шаблону договора с подстановкой реквизитов ЮЛ из профиля клиента и параметров из формы.
6. Система передает документ в ЭДО с маршрутом согласования: Клиент → Владелец.
7. Система получает от ЭДО подтверждение приема документа с идентификатором документа в ЭДО.
8. Система обновляет `[договор]`: `статус` = `на подписании клиентом`.
9. Система отправляет клиенту уведомление «договор отправлен на подпись в ЭДО».
10. Система получает от ЭДО событие «документ подписан клиентом».
11. Система обновляет `[договор]`:
    - `статус` = `на подписании парковкой`;
    - `датаПодписанияКлиентом` = момент события из ЭДО.
12. Система получает от ЭДО документ, подписанный обеими сторонами (с двумя ЭП).
13. Система сохраняет финальный документ в файловом хранилище.
14. Система обновляет `[договор]`:
    - `статус` = `активен`;
    - `файлДоговора` = ссылка на сохраненный документ;
    - `датаПодписанияПарковкой` = момент получения финального документа.
15. Система создает бронирования на указанные клиентом ПМ на весь срок договора.
16. Система отправляет клиенту уведомление об успешном заключении договора и доступности договора в ЛК.
17. Система отправляет управляющему уведомление о новом подписанном договоре.
18. Система завершает выполнение сценария.

## Расширение

**3а. У клиента уже есть договор с тем же шаблоном и пересекающимся составом машиномест за пересекающийся период**

3а1. Система определяет существующий `[договор]` по бизнес-ключу:
   - тот же `клиентИд`;
   - тот же `шаблонДоговораИд`;
   - пересекающийся состав ПМ;
   - пересекающийся период `датаНачала`–`датаОкончания`;
   - `статус` не финальный.

3а2. Система возвращает клиенту существующий с его текущим статусом.

3а3. Система завершает выполнение сценария.

**7а. Подтверждение приема от ЭДО не получено**

7а1. Система фиксирует отсутствие подтверждения приема по таймауту.  
7а2. Система отправляет клиенту уведомление об ошибке отправки документа в ЭДО.  
7а3. Система завершает выполнение сценария.

**10а. Из ЭДО получено событие отказа клиента от подписания**

10а1. Система обновляет `[договор]`: `статус` = `отклонен клиентом`.  
10а2. Система отправляет клиенту уведомление об отказе от подписания.  
10а3. Система завершает выполнение сценария.

**10б. Событие подписания клиентом не получено в течение 7 календарных дней**

10б1. Система фиксирует таймаут ожидания подписи клиента.  
10б2. Система обновляет `[договор]`: `статус` = `просрочен`.  
10б3. Система отправляет клиенту уведомление об истечении срока подписания.  
10б4. Система завершает выполнение сценария.

**12а. Из ЭДО получено событие отказа Владельца от подписания**

12а1. Система обновляет `[договор]`: `статус` = `отклонен парковкой`.  
12а2. Система отправляет клиенту уведомление об отказе со стороны парковки.  
12а3. Система завершает выполнение сценария.

**12б. Финальный документ не получен в течение 7 дней**

12б1. Система фиксирует таймаут ожидания финального документа с двумя ЭП.  
12б2. Система обновляет `[договор]`: `статус` = `просрочен`.  
12б3. Система отправляет клиенту уведомление об истечении срока подписания со стороны парковки.  
12б4. Система завершает выполнение сценария.

## Требования для UC-8.2 → интеграции

Раздел заполнится после Фазы 2 — список `INT-*`, которые будут заведены в [требованиях к интеграции](../../specs/integration/integration-requirements.md). До завершения Фазы 2 — заглушка.

## Связанные документы

- [Реестр use case](use-case-registry.md) — каноничный список вариантов использования проекта.
- [Индекс use case артефактов](readme.md) — верхний каталог use case материалов.
- [Требования к интеграции](../../specs/integration/integration-requirements.md) — общий список `INT-*`; конкретные требования для UC-8.2 появятся в Фазе 2.
- [Мастер-план UC-8.2 (интеграция с ЭДО)](../../../plans/2026-05-02-uc-8-2-edo-integration.md) — оркестрирует все 9 deliverables: UC (Фаза 1, завершена), ФТ интеграции (Фаза 2, завершена), регламент v1/v2 (Фазы 3 и 9), UML Sequence (Фаза 4), JSON+Schema (Фаза 5), XML+XSD (Фаза 6), Postman (Фаза 7), OpenAPI (Фаза 8), индексы и ретро (Фаза 10).

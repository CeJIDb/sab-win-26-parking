---
title: "Концептуальная модель системы управления парковкой с атрибутами"
type: artifact
date: 2026-03-04
source:
  - "Концептуальная модель.drawio/png"
  - "Контекстная диаграмма.md"
  - "Контекстная диаграмма MVP.md"
  - "Протоколы интервью №1.1–№5"
---

## Введение

Данный документ развивает базовую концептуальную модель системы управления парковкой и дополняет её **атрибутами ключевых сущностей**.

Цель: зафиксировать, какие данные система должна хранить и обрабатывать по каждой сущности на уровне концептуальной модели (без технических деталей реализации БД).

> ⚠️ Атрибуты приведены на основании артефактов проекта: концептуальной модели, контекстной диаграммы, протоколов интервью и связанных артефактов. Формулировки намеренно бизнес‑ориентированы.

Формат описания сущностей:

- краткое назначение;
- таблица атрибутов: название, **название на русском в camelCase** (для реализации/кода), **название в camelCase** (латиница), описание, тип/домен значений, **ограничение на значение** (из контекста проекта или предложение с пометкой), обязательность, источник.

Ограничения в столбце «Ограничение»: если указано из контекста проекта (протоколы, спецификации) — без пометки; если предложено автором модели — с пометкой «предложение:» и кратким обоснованием.

Формат столбца «Тип / домен»: *текст*, *число*, *дата*, *время*, *логический*; ссылки на сущности — &lt;Ссылка на имяСущности&gt;; перечисления — &lt;значение1, значение2, …&gt; (см. раздел со справочниками и enum).

## Справочники и перечисления (enum)

Для унификации доменов значений используются следующие перечисления (enum) и справочники.  
В этом документе:

- **перечисления (enum)** используются для статусов и технических доменов;
- **бизнес‑классификаторы** (`Тип клиента`, `Тип бронирования`, `Льготная категория`, `Юридическая форма`) трактуются как **справочники**, а не как жёстко вшитые enum (их значения могут расширяться по бизнес‑решению).

- **`Справочник: Тип клиента`**  
  - Значения: `ФЛ` (физическое лицо), `ЮЛ` (юридическое лицо).  
  - Используется в: `Клиент.Тип`.

- **`Enum: Статус эксплуатации`** (унифицированный для парковки/сектора/ПМ)  
  - Значения:  
    - `в эксплуатации` — объект доступен и используется;  
    - `закрыт на обслуживание` — временно недоступен из‑за работ;  
    - `выведен из эксплуатации` — больше не используется;  
    - `планируется к вводу` — зарезервирован, но ещё не введён в эксплуатацию.  
  - Используется в: `Парковка.Статус эксплуатации`, `Сектор.Статус эксплуатации`, `ПМ.Статус эксплуатации`.

- **`Справочник: Тип бронирования`**  
  - Значения: `автоматическое`, `краткосрочное`, `долгосрочное`.  
  - Используется в: `Бронирование.Тип`.

- **`Enum: Статус бронирования`**  
  - Значения: `черновик`, `ожидает оплаты`, `подтверждено`, `активно`, `завершено`, `отменено`, `просрочено`.  
  - Используется в: `Бронирование.Статус`.

- **`Enum: Статус парковочной сессии`**  
  - Значения: `активна`, `завершена`.  
  - Используется в: `Парковочная сессия.Статус`.

- **`Enum: Статус платежа`**  
  - Значения: `инициализирован`, `в обработке`, `успешен`, `неуспешен`, `возврат инициирован`, `возврат выполнен`.  
  - Используется в: `Платеж.Статус`.

- **`Enum: Статус фискализации`**  
  - Значения: `отправлен в ОФД`, `фискализирован`, `ошибка`, `повторная отправка`.  
  - Используется в: `Чек.Статус фискализации`.

- **`Enum: Статус договора`**  
  - Значения: `черновик`, `на согласовании`, `активен`, `приостановлен`, `расторгнут`, `архив`.  
  - Используется в: `Договор.Статус`.

- **`Enum: Статус контрагента`** (унифицирован для клиента и организации)  
  - Значения: `активен`, `архив`, `заблокирован`.  
  - Используется в: `Клиент.Статус`, `Организация.Статус`.

- **`Enum: Статус обращения`**  
  - Значения: `новое`, `в работе`, `ожидает клиента`, `решено`, `отклонено`, `закрыто`.  
  - Используется в: `Обращение.Статус`.

- **`Enum: Статус доставки уведомления`**  
  - Значения: `успешно`, `ошибка`.  
  - Используется в: `Уведомление.Статус доставки`.

- **`Enum: Тип уведомления`**  
  - Значения: `по договору`, `по оплате`, `по задолженности`, `по доступу`, `по бронированию`, `по парковочной сессии`, `по обращениям`, `маркетинг`, `иное`.  
  - Используется в: `Шаблон уведомления.Тип` (тип события задаётся шаблоном; у экземпляра уведомления тип выводится из шаблона). Соответствует типам из Контекстной диаграммы и протоколов (напоминания об оплате, окончание договора, ремонты, ответы по обращениям).

- **`Enum: Тип обращения`**  
  - Значения: `вопрос`, `жалоба`, `претензия`, `предложение` (в соответствии с формой создания обращения в ЛК).  
  - Используется в: `Обращение.Тип`.

- **`Enum: Тип связанного объекта обращения`**  
  - Значения: `бронирование`, `парковочнаяСессия`, `платеж`, `чек`, `договор`.  
  - Используется в: `Обращение.Тип связанного объекта`.

- **`Enum: Канал взаимодействия`** (унифицирован для обращений и уведомлений)  
  - Значения: `личный кабинет`, `звонок`, `e‑mail`, `SMS`.  
  - Используется в:  
    - `Обращение.Канал` (канал, по которому поступило обращение),  
    - `Уведомление.Канал` (канал, по которому отправлено уведомление),  
    - `Настройки уведомлений.Каналы` (каналы, по которым клиент согласен получать уведомления).

- **`Enum: Тип согласия`**  
  - Значения: `согласие с офертой`, `обработка ПДн`, `маркетинг`.  
  - Используется в: `Согласие.Тип согласия`.

- **`Enum: Тип КПП`**  
  - Значения: `въезд`, `выезд`, `двусторонний`.  
  - Используется в: `КПП.Тип`.

- **`Enum: Роль сотрудника`**  
  - Значения: `охранник`, `управляющий`, `владелец`.  
  - Используется в: `Сотрудник.Роль`.

- **`Enum: Статус сотрудника`**  
  - Значения: `активен`, `заблокирован`.  
  - Используется в: `Сотрудник.Статус`.

- **`Enum: Тип тарифа`**  
  - Значения: `почасовой`, `суточный`, `абонемент`, `штрафной`.  
  - Используется в: `Тариф.Тип`.

- **`Справочник: Льготная категория`**  
  - Значения: `ОВЗ`, `пенсионер`, `ветеран`, `многодетная семья`, `сотрудник оператора`, `иная льгота`.  
  - Используется в: `Клиент.Льготная категория`, `Льготный документ.Льготная категория`, `Тариф.Льготная категория`.

- **`Справочник: Юридическая форма`**  
  - Значения: `ООО`, `АО`, `ПАО`, `ИП`, `НКО`, `иная`.  
  - Используется при описании типов юридических лиц (ООО, ИП и т.п.) в контрагентских данных.

Отдельные домены, реализованные как сущности‑справочники (`Тип зоны`, `Тип ТС`), используются по ссылке, а не как enum‑значения.

---

## Сущности и атрибуты

### `Парковка`

**Назначение.** Парковочный объект (площадка/комплекс), в рамках которого организованы парковочные места и действует тарифная политика.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| ---------------------| -------------------------| ---------------------| ---------------------------------------------------| ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор парковки | *число* | Формат UUID (предложение: однозначность) | Да | Концептуальная модель |
| Наименование | наименование | name | Название парковки | *текст* | Длина ≤ 255 (предложение: типичный лимит для названий) | Да | Интервью, карточка проекта |
| Адрес | адрес | address | Физический адрес | *текст* | Длина ≤ 500 (предложение: адрес одной строкой) | Да | Контекстная диаграмма, интервью |
| Тип | тип | type | Тип парковки (открытая, крытая и т.п.) | <открытая, крытая> | Значение из справочника | Нет | Интервью |
| Описание | описание | description | Текстовое описание / примечания | *текст* | Длина ≤ 2000 (предложение: ограничение объёма текста) | Нет | Интервью |
| Временной режим | временнойРежим | workingHours | Часы работы | *текст* | Длина ≤ 100 (предложение: например «9:00–21:00») | Нет | Контекстная диаграмма |
| Статус эксплуатации | статусЭксплуатации | operationalStatus | Эксплуатационный статус парковки: активна, закрыта на реконструкцию и т.п. | <в эксплуатации, закрыт на обслуживание, выведен из эксплуатации, планируется к вводу> | Значение из Enum: Статус эксплуатации | Да | Операционная модель |

**Связи:**

- `Парковка` —(1:N)→ `Сектор` — одна парковка содержит несколько секторов.
- `Парковка` —(1:N)→ `КПП` — у парковки может быть несколько КПП (точек въезда/выезда).

---

### `Сектор`

**Назначение.** Логически/физически выделенная часть парковки (зона, этаж и т.п.).

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| ---------------------| ---------------------------| ---------------------| ---------------------------------------------------| ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор сектора | *число* | Формат UUID | Да | Концептуальная модель |
| Парковка | парковкаИд | parkingId | Ссылка на парковку | <Ссылка на Парковка> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Наименование | наименование | name | Человекочитаемое обозначение (А, B, Этаж 1 и т.п.) | *текст* | Длина ≤ 50 (предложение: короткое обозначение) | Да | Схема парковки AS‑IS |
| Тип зоны | типЗоныИд | zoneTypeId | Краткосрочная, долгосрочная, ОВЗ, мото+резерв и т.п. | <Ссылка на Тип зоны> | внешний ключ на существующую запись | Да | Контекстная диаграмма, AS‑IS |
| Статус эксплуатации | статусЭксплуатации | operationalStatus | Эксплуатационный статус сектора: активен, закрыт на обслуживание и т.п. | <в эксплуатации, закрыт на обслуживание, выведен из эксплуатации, планируется к вводу> | Значение из Enum: Статус эксплуатации | Да | Операционная модель |

**Связи:**

- `Сектор` ←(N:1)— `Парковка` — каждый сектор принадлежит одной парковке.
- `Сектор` —(1:N)→ `ПМ` — сектор включает множество парковочных мест.
- `Сектор` ←(N:1)— `Тип зоны` — каждый сектор имеет один тип зоны.

---

### `Тип зоны`

**Назначение.** Справочник типов зон парковки, определяющий бизнес‑режим использования территории и применимые правила тарификации.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| ---------------------| ---------------------------| ---------------------| ---------------------------------------------------| ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор типа зоны | *число* | Формат UUID | Да | Концептуальная модель |
| Код | код | code | Внутренний код типа зоны (например, `SHORT_TERM`, `LONG_TERM`, `OVZ`, `MOTO_RESERVE`) | *текст* | Длина ≤ 50, уникальность (предложение) | Да | Внутренний справочник |
| Наименование | наименование | name | Человекочитаемое название (краткосрочная, долгосрочная, ОВЗ, мото+резерв и т.п.) | *текст* | Длина ≤ 255 | Да | Контекстная диаграмма, схема AS‑IS |
| Описание | описание | description | Описание бизнес‑режима зоны и общих правил использования | *текст* | Длина ≤ 2000 (предложение) | Нет | Протоколы, бизнес‑правила |

**Связи:**

- `Тип зоны` —(1:N)→ `Сектор` — один тип зоны может быть назначен многим секторам.
- `Тип зоны` —(1:N)→ `Разрешённый тип ТС в типе зоны` — задаёт, какие типы ТС разрешены в этой зоне.
- `Тип зоны` —(1:N)→ `Применимость тарифа к типу зоны` — один тип зоны может иметь несколько записей применимости тарифов.

---

### `Тип ТС`

**Назначение.** Справочник типов/категорий транспортных средств, используемый для зонирования и тарификации.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| ---------------------| ---------------------------| ---------------------| ---------------------------------------------------| ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор типа ТС | *число* | Формат UUID | Да | Концептуальная модель |
| Код | код | code | Внутренний код типа ТС (например, `CAR`, `MOTO`, `MUNICIPAL`) | *текст* | Длина ≤ 50, уникальность (предложение) | Да | Внутренний справочник |
| Наименование | наименование | name | Название типа ТС (легковой, мотоцикл, муниципальная техника и т.п.) | *текст* | Длина ≤ 255 (предложение) | Да | Контекстная диаграмма |
| Описание | описание | description | Дополнительное описание особенностей категории | *текст* | Длина ≤ 1000 (предложение) | Нет | Протоколы |

**Связи:**

- `Тип ТС` —(1:N)→ `Разрешённый тип ТС в типе зоны` — используется при определении допустимых типов ТС в зоне.
- `Тип ТС` —(1:0..N)→ `ТС` — задаёт категорию конкретных транспортных средств.

---

### `Разрешённый тип ТС в типе зоны`

**Назначение.** Связующая сущность, задающая, какие типы ТС разрешены для каждого типа зоны (нормализация связи «Тип зоны → множество Типов ТС»).

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| ---------------------| ---------------------------| ---------------------| ---------------------------------------------------| ------------------------------------| ----------- | ---------- | --------------------------------|
| Тип зоны | типЗоныИд | zoneTypeId | Тип зоны, для которого задаётся разрешённый тип ТС | <Ссылка на Тип зоны> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Тип ТС | типТсИд | vehicleTypeId | Тип транспортного средства, разрешённый в этой зоне | <Ссылка на Тип ТС> | внешний ключ на существующую запись; пара (zoneTypeId, vehicleTypeId) уникальна (предложение) | Да | Концептуальная диаграмма |

> Для каждого `Тип зоны` может быть несколько связанных записей `Разрешённый тип ТС в типе зоны` (разные `Тип ТС`). Кардинальность: `Тип зоны` (1) — (N) `Разрешённый тип ТС в типе зоны` — (N) (1) `Тип ТС`.

**Связи:**

- `Разрешённый тип ТС в типе зоны` ←(N:1)— `Тип зоны`.
- `Разрешённый тип ТС в типе зоны` ←(N:1)— `Тип ТС`.

---

### `Применимость тарифа к типу зоны`

**Назначение.** Нормализующая сущность для связи «`Тип зоны` — `Тариф`» многие‑ко‑многим: задаёт, какие тарифы применимы к каким типам зон.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| ---------- | -------------------------| ------------------- | -------------------------------------------------- | -------------------- | ----------- | ---------- | ----------------------|
| Тип зоны | типЗоныИд | zoneTypeId | Тип зоны, к которому применяется тариф | <Ссылка на Тип зоны> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Тариф | тарифИд | tariffId | Тариф, применимый к указанному типу зоны | <Ссылка на Тариф> | внешний ключ на существующую запись; пара (zoneTypeId, tariffId) уникальна (предложение) | Да | Концептуальная модель |

**Связи:**

- `Применимость тарифа к типу зоны` ←(N:1)— `Тип зоны` — один тип зоны может иметь несколько применимых тарифов.
- `Применимость тарифа к типу зоны` ←(N:1)— `Тариф` — один тариф может применяться к нескольким типам зон.

---

### `ПМ` (Парковочное место)

**Назначение.** Конкретное физическое место для размещения транспортного средства.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| ---------------------| ---------------------------| ---------------------| ---------------------------------------------------| ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор ПМ | *число* | Формат UUID | Да | Концептуальная модель |
| Сектор | секторИд | sectorId | Ссылка на сектор | <Ссылка на Сектор> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Номер места | номерМеста | placeNumber | Номер/обозначение на схеме | *текст* или *число* | Длина ≤ 20 или целое 1–99999 (предложение: уникальность в рамках сектора) | Да | Схема парковки AS‑IS |
| Бронирование | текущееБронированиеИд | currentBookingId | Ссылка на действующее долгосрочное бронирование, закрепляющее место за клиентом (**операционный атрибут/проекция**: поддерживается логикой приложения на основе записей `Бронирование`) | <Ссылка на Бронирование> | внешний ключ или null | Нет | Интервью ЮЛ, концепция долгосрочных бронирований |
| Признак зарезервированности | зарезервировано | reserved | Признак того, что место зарезервировано по активному бронированию (включая краткосрочные и автоматические) | *логический* | **Операционный атрибут:** вычисляется/поддерживается на основе активных `Бронирование` для данного ПМ/Сектора | Нет | ES TO‑BE, UC‑7.x, UC‑12.2 |
| Признак фактической занятости | фактическиЗанято | physicallyOccupied | Признак того, что место фактически занято (есть активная `Парковочная сессия`, связанная через `Бронирование`) | *логический* | **Операционный атрибут:** вычисляется/поддерживается на основе активных `Парковочная сессия` для данного ПМ/Сектора | Нет | ES TO‑BE, UC‑12.4, UC‑12.9 |
| Статус эксплуатации | статусЭксплуатации | operationalStatus | Эксплуатационный статус места: доступно, заблокировано (техобслуживание), выведено из эксплуатации и т.п. | <в эксплуатации, закрыт на обслуживание, выведен из эксплуатации, планируется к вводу> | Значение из Enum: Статус эксплуатации | Да | Операционная модель |
 
> Тип зоны и разрешённые типы ТС для ПМ наследуются от связанного `Сектор` (зонирование и ограничения по типам ТС задаются на уровне сектора; отдельные отклонения моделируются как отдельные секторы).  
> Операционные состояния ПМ разделены: «Зарезервировано» (атрибут `зарезервировано`) отражает наличие активных `Бронирование` (в т.ч. краткосрочных/автоматических), а «Занято» (атрибут `фактическиЗанято`) — наличие активных `Парковочная сессия`. Оба атрибута могут поддерживаться как производные/кэшируемые на уровне доменного сервиса. `Статус эксплуатации` по‑прежнему описывает именно пригодность места к использованию (доступно/заблокировано/выведено из эксплуатации и т.п.).

**Связи:**

- `ПМ` ←(N:1)— `Сектор` — каждое место принадлежит одному сектору.
- `ПМ` —(1:N)→ `Бронирование` — на одно место может быть несколько бронирований в разные периоды.
- `ПМ` ←(N:1)— `Тариф` (опционально) — место может наследовать/иметь свой тариф.

---

### `Бронирование`

**Назначение.** Запись о заранее зарезервированном использовании парковочного пространства на период: **краткосрочное бронирование резервирует слот в секторе**, а **долгосрочное/договорное — конкретное парковочное место (ПМ) для конкретного ТС**.  
`Бронирование` является **мастер‑сущностью для финансов и прав доступа**: все суммы, платежи, чеки и договорные ограничения (квоты, тарифы, льготы) привязаны именно к бронированию, а не к парковочной сессии.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор бронирования | *число* | Формат UUID | Да | Концептуальная модель |
| ТС | тсИд | vehicleId | Транспортное средство, для которого создано бронирование | <Ссылка на ТС> | внешний ключ на существующую запись | Да | Протоколы, договорённости по привязке к ТС |
| Сектор | секторИд | sectorId | Целевая зона/сектор, в котором резервируется слот (для краткосрочных бронирований; для долгосрочных определяется через `ПМ`) | <Ссылка на Сектор> | внешний ключ на существующую запись | Да | Концептуальная модель, интервью |
| ПМ | парковочноеМестоИд | parkingPlaceId | Ссылка на конкретное парковочное место (для долгосрочных/договорных бронирований; для краткосрочных может быть заполнено при фактическом назначении места) | <Ссылка на ПМ> | внешний ключ или null | Нет | Концептуальная модель, интервью |
| Договор | договорИд | contractId | Договор, в рамках которого создано долгосрочное/договорное бронирование (для краткосрочных не заполняется) | <Ссылка на Договор> | внешний ключ или null | Нет | Концептуальная модель, интервью |
| Дата начала | датаНачала | startDate | Дата/время начала брони | *дата*, *время* | Не в прошлом при создании (предложение) | Да | Контекстная диаграмма |
| Длительность | длительность | duration | Планируемая длительность бронирования, в минутах | *число* | > 0 (предложение: положительное целое, единица — минуты) | Да | Контекстная диаграмма, интервью |
| Тип | тип | type | Автоматическое, краткосрочное, долгосрочное | <автоматическое, краткосрочное, долгосрочное> | Значение из справочника | Да | Интервью, протоколы |
| Статус | статус | status | Создано, подтверждено, активно, завершено, отменено и т.п. | <черновик, ожидает оплаты, подтверждено, активно, завершено, отменено, просрочено> | Значение из Enum | Да | Бизнес‑правила |
| Тариф | тарифИд | tariffId | Ссылка на примененный тариф | <Ссылка на Тариф> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Сумма к оплате | суммаКОплате | amountDue | Итоговая сумма за бронирование (**результат тарификации** по применённому `Тариф`, дате начала, длительности и льготам; хранится как зафиксированный результат расчёта) | *число* | ≥ 0 (предложение: неотрицательная сумма) | Нет | Контекстная диаграмма, правила тарификации |

Связь с конкретной парковкой и типом зоны для краткосрочных бронирований задаётся через атрибут `Сектор` → `Парковка`/`Тип зоны`; для долгосрочных/договорных — через `ПМ` → `Сектор` → `Парковка`/`Тип зоны`. Отдельных атрибутов "Зона"/"Тип зоны" в бронировании не хранится, они определяются по связанным объектам. Для любого бронирования действует бизнес‑правило: **`Сектор` обязателен всегда; `ПМ` обязателен только для долгосрочных/договорных бронирований (для краткосрочных может оставаться пустым)**. Клиент определяется через `ТС` → `Клиент`.  
**Инвариант:** все платежи, суммы и фискальные документы по парковке проводятся в контексте конкретного `Бронирование`; `Парковочная сессия` используется только как факт времени/КПП и, при необходимости, аналитический след суммы по факту, но не как финансовый мастер.

**Связи:**

- `Бронирование` ←(0..N:1)— `ТС` — одно ТС может иметь ноль или много связанных бронирований; каждое бронирование относится к одному ТС.
- `Бронирование` ←(0..N:1)— `Сектор` — бронирование может ссылаться на сектор (для краткосрочных бронирований); у сектора может быть много бронирований или ни одного.
- `Бронирование` ←(0..N:1)— `ПМ` — бронирование может закреплять конкретное парковочное место (для долгосрочных/договорных бронирований) или не иметь ПМ (для краткосрочных).
- `Бронирование` ←(0..N:1)— `Договор` — долгосрочные/договорные бронирования оформляются в рамках договора; краткосрочные могут не иметь связи с договором.
- `Бронирование` —(1:0..N)→ `Парковочная сессия` — одно бронирование может не породить ни одной сессии или несколько сессий; каждая сессия создаётся в контексте конкретного бронирования (в т.ч. созданного на лету при въезде без предварительной брони; см. ADR‑002, Option D).
- `Бронирование` ←(0..N:1)— `Тариф` — стоимость бронирования рассчитывается по тарифу; один тариф может использоваться во множестве бронирований.
- `Бронирование` —(1:1..N)→ `Платеж` — бронирование может быть оплачено одним или несколькими платежами.
- `Бронирование` —(0..1:0..N)→ `Уведомление` — с бронированием может быть связано ноль или несколько уведомлений.
- `Бронирование` —(0..1:0..N)→ `Обращение` — по бронированию может не быть ни одного обращения или несколько обращений.

---

### `Парковочная сессия`

**Назначение.** Фактический период нахождения ТС на парковке (от въезда до выезда), зафиксированный для конкретного бронирования: используется для учёта времени пребывания и анализа работы КПП; финансовый результат фиксируется на уровне `Бронирование` и связанных `Платеж`. `Парковочная сессия` не имеет собственных прямых ссылок на `ТС`, `Тариф`, `Договор`, `Клиент`, а использует связанные данные только через `Бронирование`.  
При необходимости `Парковочная сессия` может хранить **аналитическую сумму по факту**, фиксирующую «как посчитали по факту» по результатам тарификации по фактической длительности сессии; при этом мастер‑суммой для платёжных обязательств и чеков остаётся сумма на уровне `Бронирование`.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор сессии | *число* | Формат UUID | Да | Концептуальная модель |
| Бронирование | бронированиеИд | bookingId | Бронирование, в рамках которого идёт сессия | <Ссылка на Бронирование> | внешний ключ на существующую запись | Да | Концептуальная модель, ADR‑002 |
| Время въезда | времяВъезда | entryTime | Зафиксированное время въезда | *дата*, *время* | Не в будущем (предложение) | Да | Контекстная диаграмма, СКУД |
| Время выезда | времяВыезда | exitTime | Зафиксированное время выезда | *дата*, *время* | ≥ время въезда (предложение) | Нет | Контекстная диаграмма, СКУД |
| Длительность | длительность | durationMinutes | Фактическая длительность сессии между временем въезда и временем выезда, в минутах (**derived** из `времяВъезда` и `времяВыезда`; хранится для удобства аналитики и тарификации) | *число* | ≥ 0 (предложение: неотрицательное целое, минуты) | Нет | Бизнес‑правила тарификации, ES TO‑BE |
| КПП въезда | кппВъездаИд | entryKppId | КПП, через который выполнен въезд | <Ссылка на КПП> | внешний ключ или null | Нет | Концептуальная модель |
| КПП выезда | кппВыездаИд | exitKppId | КПП, через который выполнен выезд | <Ссылка на КПП> | внешний ключ или null | Нет | Концептуальная модель |
| Способ допуска | способДопуска | accessMethod | Как было принято решение о допуске ТС: автоматически по правилам доступа или вручную сотрудником | <автоматически, вручную> | Значение из Enum | Да | Бизнес‑правила, UI КПП |
| Сотрудник | сотрудникИд | employeeId | Сотрудник, принявший решение о ручном допуске/коррекции (для автоматических сценариев не заполняется) | <Ссылка на Сотрудник> | внешний ключ или null | Нет | Концептуальная модель, UI КПП |
| Комментарий допуска | комментарийДопуска | accessComment | Комментарий сотрудника о причине ручного допуска/коррекции | *текст* | Длина ≤ 500 (предложение) | Нет | Протоколы, UI КПП |
| Статус | статус | status | Активна, завершена | <активна, завершена> | Значение из Enum | Да | Бизнес‑правила |

Бизнес‑правило реализации (ADR‑002, Option D): **каждая `Парковочная сессия` должна иметь ссылку на `Бронирование` (в т.ч. созданное на лету при въезде без предварительной брони)**. Информация о ТС, секторе/ПМ, тарифе и договоре для сессии всегда берётся только через связанное `Бронирование`; прямых связей с этими сущностями у `Парковочная сессия` нет.

**Связи:**

- `Парковочная сессия` ←(N:1)— `Бронирование` — каждая сессия относится к одному бронированию; одно бронирование может иметь ноль или несколько сессий.
- `Парковочная сессия` ←(0..1:1)— `КПП` (КПП въезда) и `КПП` (КПП выезда) — сессия хранит КПП въезда и выезда.
- `Парковочная сессия` ←(0..N:0..1)— `Сотрудник` — при ручных сценариях допуска сессия хранит, какой сотрудник принял решение и оставил комментарий.

---

### `Платеж`

**Назначение.** Факт оплаты услуг (парковки, бронирования и др.) со статусом.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор платежа | *число* | Формат UUID | Да | Концептуальная модель |
| Бронирование | бронированиеИд | bookingId | Бронирование, к которому относится платёж (все платежи по парковке проводятся в контексте конкретного бронирования) | <Ссылка на Бронирование> | внешний ключ на существующую запись | Да | Концептуальная модель, правила тарификации |
| Сумма | сумма | amount | Сумма платежа | *число* | > 0 (предложение: положительная сумма) | Да | Контекстная диаграмма |
| Валюта | валюта | currency | Валюта расчёта | <справочник> | Значение из справочника (например RUB) | Да | Общие требования |
| Способ оплаты | способОплаты | paymentMethod | Онлайн, терминал, безнал по договору и т.п. | <справочник> | Значение из справочника | Да | Контекстная диаграмма |
| Статус | статус | status | Инициализирован, успешно, неуспешно, возврат и т.п. | <инициализирован, в обработке, успешен, неуспешен, возврат инициирован, возврат выполнен> | Значение из Enum: Статус платежа | Да | Интеграция с платёжной системой |
| Время инициации | времяИнициации | initiatedAt | Дата/время начала платежа | *дата*, *время* | Не в будущем (предложение) | Да | Контекстная диаграмма |
| Время завершения | времяЗавершения | completedAt | Дата/время завершения | *дата*, *время* | ≥ время инициации (предложение) | Нет | Контекстная диаграмма |
| Идентификатор провайдера | провайдерИд | providerId | Внешний id платежа | *текст* | Длина ≤ 100 (предложение: внешний ключ провайдера) | Да | Интеграция с платёжной системой |

**Связи:**

- `Платеж` ←(N:1)— `Бронирование` — оплата конкретного бронирования.
- `Платеж` —(1:0..1)→ `Чек` — успешный платеж порождает не более одного фискального чека; при неуспешном платеже чек не формируется.
- `Платеж` —(0..1:0..N)→ `Уведомление` — платеж может породить ноль или несколько уведомлений о списании, возврате, неуспехе.
- `Платеж` —(0..1:0..N)→ `Обращение` (опционально) — по платежу может не быть ни одного обращения или несколько обращений.

---

### `Чек`

**Назначение.** Фискальный/расчётный документ, подтверждающий платеж.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор чека | *число* | Формат UUID | Да | Концептуальная модель |
| Платеж | платёжИд | paymentId | Ссылка на платеж | <Ссылка на Платеж> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Номер фискальный | номерФискальный | fiscalNumber | Номер/реквизиты фискального чека | *текст* | Длина ≤ 50 (предложение: формат ОФД) | Да | Интеграция с ОФД |
| Дата/время чека | датаВремяЧека | receiptAt | Момент формирования | *дата*, *время* | Не в будущем | Да | Интеграция с ОФД |
| Статус фискализации | статусФискализации | fiscalStatus | Успешно, ошибка, повторная отправка и т.п. | <отправлен в ОФД, фискализирован, ошибка, повторная отправка> | Значение из Enum: Статус фискализации | Да | Контекстная диаграмма |
| Сумма | сумма | amount | Сумма по чеку | *число* | ≥ 0 (предложение) | Да | ОФД |

**Связи:**

- `Чек` ←(0..1:1)— `Платеж` — чек связан с платежом.

---

### `Шаблон договора`

**Назначение.** Типовой образец текста и условий договора, по которому формируются конкретные договоры с клиентами. Версионирование шаблона позволяет фиксировать, какие условия применены к договору.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| ------------------| ---------------------------| ---------------------| -------------------------------------------------------------------------| ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор шаблона договора | *число* | Формат UUID | Да | Концептуальная модель |
| Код | код | code | Внутренний код шаблона (например, `ABONEMENT_FL`, `CORP_AGREEMENT`) | *текст* | Длина ≤ 50, уникальность (предложение) | Да | ЭДО, биллинг |
| Наименование | наименование | name | Человекочитаемое название шаблона (Абонемент ФЛ, Договор с ЮЛ и т.п.) | *текст* | Длина ≤ 255 (предложение) | Да | Интервью, протоколы |
| Версия | версия | version | Версия шаблона для учёта изменений условий | *текст* или *дата* | Длина ≤ 20 (предложение) | Нет | ЭДО, версионирование |
| Тип | тип | type | Бизнес-тип шаблона: абонемент ФЛ, абонемент ЮЛ, корпоративный, гостевой и т.п. | *текст* или <справочник> | Значение из справочника | Нет | Протоколы, биллинг |
| *текст* | текст | body | Шаблон текста договора с плейсхолдерами (реквизиты клиента, парковка, тарифы, даты) | *текст* | Длина ≤ 50 000 (предложение: объём типового договора) | Нет | ЭДО, интервью |
| Дата начала действия | датаНачалаДействия | effectiveFrom | С какой даты шаблон действует (для версионирования) | *дата* | Дата окончания действия > даты начала при заполнении (предложение) | Нет | ЭДО |
| Дата окончания действия | датаОкончанияДействия | effectiveTo | По какую дату шаблон действует (если выведен из использования) | *дата* | — | Нет | ЭДО |

**Связи:**

- `Шаблон договора` —(1:0..N)→ `Договор` — по одному шаблону может быть сформировано ноль или много договоров; каждый договор ссылается не более чем на один шаблон.

---

### `Договор`

**Назначение.** Юридическое соглашение между клиентом и оператором парковки, сформированное на основе шаблона договора (при наличии).

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор договора | *число* | Формат UUID | Да | Концептуальная модель |
| Шаблон договора | шаблонДоговораИд | contractTemplateId | Шаблон, по которому сформирован договор | <Ссылка на Шаблон договора> | внешний ключ или null | Нет | ЭДО, биллинг |
| Клиент | клиентИд | clientId | Клиент‑владелец договора (ФЛ/ЮЛ) | <Ссылка на Клиент> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Номер договора | номерДоговора | contractNumber | Внутренний/юридический номер | *текст* | Длина ≤ 50, уникальность (предложение) | Да | ЭДО, биллинг |
| Дата начала | датаНачала | startDate | Дата вступления в силу | *дата* | Не в будущем при активации (предложение) | Да | Интервью, протоколы |
| Дата окончания | датаОкончания | endDate | Дата окончания / бессрочно | *дата* | ≥ дата начала при заполнении (предложение) | Нет | Интервью |
| Статус | статус | status | Черновик, активен, приостановлен, расторгнут | <черновик, на согласовании, активен, приостановлен, расторгнут, архив> | Значение из Enum: Статус договора | Да | Бизнес‑правила |
| Файл договора | файлДоговора | documentFileRef | Ссылка/идентификатор файла подписанного договора во внешнем хранилище (ЭДО или файловый сторедж; хранится ссылка/ключ, а не бинарный файл) | *текст* | Длина ≤ 500 (предложение: URL или внешний идентификатор) | Нет | Контекстная диаграмма, интеграция с ЭДО |

Делегация парковочных мест и квот по договору клиента‑ЮЛ реализуется через набор связанных записей `Бронирование` по этому договору (закрепление конкретных ПМ и/или квоты сессий за ТС/водителями в рамках договора).

> В контексте проекта (Контекстная диаграмма, интеграция ЭДО) подписанный документ договора может храниться во внешней системе ЭДО. При необходимости явной ссылки на документ в системе можно ввести опциональный атрибут «Ссылка на документ ЭДО» (идентификатор/URL документа в ЭДО).

**Связи:**

- `Договор` ←(0..N:1)— `Клиент` — договор принадлежит одному клиенту (ФЛ/ЮЛ); у клиента может быть ноль или несколько договоров.
- `Договор` ←(0..N:1)— `Шаблон договора` — договор может быть сформирован по шаблону или без него (ручное оформление).
- `Договор` —(0..1:1..N)→ `Бронирование` (для долгосрочных бронирований) — один договор может иметь ноль или много связанных бронирований; каждое долгосрочное бронирование (>24 часов) может быть привязано не более чем к одному действующему договору.
- `Договор` —(1:N)→ `Уведомление` (опционально) — сообщения о статусе договора.
- `Договор` —(1:N)→ `Обращение` (опционально) — вопросы по договору.

---

### `Организация`

**Назначение.** Юридическое лицо как объект учёта: используется для хранения реквизитов и данных организации‑клиента (ЮЛ).

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор организации | *число* | Формат UUID | Да | Концептуальная модель, интервью |
| Наименование | наименование | name | Полное/краткое наименование организации | *текст* | Длина ≤ 500 (предложение) | Да | Контекстная диаграмма |
| Юридическая форма | юридическаяФорма | legalForm | Организационно‑правовая форма (ООО, АО, ИП и т.п.) | <ООО, АО, ПАО, ИП, НКО, иная> | Значение из справочника | Нет | Интервью |
| Юридический адрес | юридическийАдрес | legalAddress | Юридический адрес | *текст* | Длина ≤ 500 (предложение) | Да | Контекстная диаграмма |
| Фактический адрес | фактическийАдрес | actualAddress | Фактический адрес (если отличается) | *текст* | Длина ≤ 500 (предложение) | Нет | Интервью |
| ИНН | инн | inn | Идентификационный номер налогоплательщика | *текст* | 10 цифр (ЮЛ) или 12 цифр (ИП); формат РФ (предложение) | Да | Контекстная диаграмма |
| КПП | кпп | kpp | Код причины постановки на учёт | *текст* | 9 цифр; формат РФ (предложение) | Нет | Контекстная диаграмма |
| ОГРН / ОГРНИП | огрн | ogrn | Регистрационный номер | *текст* | 13 или 15 цифр; формат РФ (предложение) | Нет | Интервью |
| Банковские реквизиты | банковскиеРеквизиты | bankDetails | Р/с, банк, БИК и т.п. | *текст* | Структура: р/с ≤ 20 цифр, БИК 9 цифр (предложение) | Да | Контекстная диаграмма |
| E-mail | email | email | Контактный e‑mail организации | *текст* | Формат email: обязательный символ `@`, до `@` минимум 1 символ, после `@` минимум 3 символа и хотя бы одна точка `.` в доменной части; длина 5–255 (предложение) | Нет | Контекстная диаграмма |
| Телефон | телефон | phone | Контактный телефон организации | *текст* | Длина ≤ 20 (предложение: E.164 или национальный формат) | Нет | Контекстная диаграмма |
| Статус | статус | status | Активна, архив и т.п. | <активен, архив, заблокирован> | Значение из Enum | Нет | Бизнес‑правила |

**Связи:**

- `Организация` —(0..1:1)→ `Клиент` — организация может быть привязана к одному клиенту типа ЮЛ или не использоваться; каждый клиент‑ЮЛ ссылается не более чем на одну организацию.

---

### `Паспортные данные`

**Назначение.** Чувствительные реквизиты документа, удостоверяющего личность клиента‑ФЛ (паспорт и аналогичные документы), используемые для идентификации и договорной работы.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор записи паспортных данных | *число* | Формат UUID | Да | Концептуальная модель, интервью |
| Тип документа | типДокумента | documentType | Паспорт РФ, загранпаспорт, иной документ | <справочник> | Значение из справочника | Да | Требования по идентификации |
| Серия и номер | серияИНомер | seriesAndNumber | Серия и номер документа | *текст* | Паспорт РФ: 4 цифры + 6 цифр (предложение) | Да | Требования по ПДн |
| Дата выдачи | датаВыдачи | issueDate | Дата выдачи документа | *дата* | Не в будущем (предложение) | Да | Требования по идентификации |
| Кем выдан | кемВыдан | issuedBy | Орган, выдавший документ | *текст* | Длина ≤ 500 (предложение) | Да | Требования по идентификации |
| Код подразделения | кодПодразделения | departmentCode | Код подразделения/органа | *текст* | Длина ≤ 10 (предложение: формат РФ XXX-XXX) | Да | Требования по идентификации |

> Все поля сущности «Паспортные данные» относятся к чувствительной персональной информации и на уровне реализации должны храниться в зашифрованном виде с ограничением доступа.

**Связи:**

- `Паспортные данные` ←(0..1:1)— `Клиент` — у одного клиента‑ФЛ может быть ноль или одна запись паспортных данных; каждая запись относится к одному клиенту.

---

### `Льготный документ`

**Назначение.** Документ, подтверждающий право клиента на льготу (удостоверение, справка, карта и т.п.) с указанием реквизитов и образа документа.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор льготного документа | *число* | Формат UUID | Да | Концептуальная модель, интервью |
| Льготная категория | льготнаяКатегория | benefitCategory | Категория льготы, которую подтверждает документ | <ОВЗ, пенсионер, ветеран, многодетная семья, сотрудник оператора, иная льгота> | Значение из справочника | Да | Контекстная диаграмма |
| Тип документа | типДокумента | documentType | Удостоверение, справка, карта и т.п. | *текст* или <справочник> | Длина ≤ 100 (предложение) | Да | Требования по льготам |
| Номер/реквизиты | номерИлиРеквизиты | numberOrDetails | Номер или иные реквизиты документа | *текст* | Длина ≤ 100 (предложение) | Нет | Требования по льготам |
| Дата выдачи | датаВыдачи | issueDate | Дата выдачи льготного документа | *дата* | Не в будущем (предложение) | Нет | Требования по льготам |
| Дата окончания | датаОкончания | expiryDate | Дата окончания действия льготы | *дата* | ≥ дата выдачи при заполнении (предложение) | Нет | Требования по льготам |
| Образ документа | образДокументаИд | documentImageId | Ссылка на файл/образ документа (фото/скан) | *текст* | Длина ≤ 500 (предложение) | Нет | Требования по льготам |
| Статус проверки | статусПроверки | verificationStatus | Не предоставлен, на проверке, подтверждён, отклонён | <справочник> | Значение из справочника | Нет | Бизнес‑правила |

> Образ документа и реквизиты относятся к персональным данным и на уровне реализации должны храниться с учётом требований по защите ПДн (ограничение доступа, шифрование, маскирование в интерфейсах).

**Связи:**

- `Льготный документ` ←(0..1:1)— `Клиент` — у клиента может быть ноль или один актуальный льготный документ; каждая запись относится к одному клиенту.

---

### `Согласие`

**Назначение.** Юридические согласия клиента (согласие с офертой, обработка персональных данных, маркетинговые рассылки) с фиксацией даты предоставления и отзыва для аудита и соответствия требованиям по ПДн.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| --------------------| ---------------------------| ---------------------| -------------------------------------------------------------------------| ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор записи согласия | *число* | Формат UUID | Да | Концептуальная модель |
| Клиент | клиентИд | clientId | Клиент, давший согласие | <Ссылка на Клиент> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Тип согласия | типСогласия | consentType | Вид согласия: согласие с офертой, обработка ПДн, маркетинг | <согласие с офертой, обработка ПДн, маркетинг> | Значение из Enum | Да | Требования по ПДн, протоколы |
| Признак согласия | признакСогласия | consentGiven | Согласие предоставлено (да) или отозвано (нет) | *логический* | true/false | Да | Требования по ПДн |
| Дата предоставления | датаПредоставления | givenAt | Дата и время предоставления согласия | *дата*, *время* | Не в будущем (предложение) | Нет | Требования по ПДн |
| Дата отзыва | датаОтзыва | revokedAt | Дата и время отзыва согласия (если отозвано) | *дата*, *время* | ≥ дата предоставления при заполнении (предложение) | Нет | Требования по ПДн |

> Для аудита и соответствия регуляторным требованиям рекомендуется хранить историю: при отзыве не удалять запись, а фиксировать дату отзыва и менять признак; при повторном предоставлении — создавать новую запись.

**Связи:**

- `Согласие` ←(N:1)— `Клиент` — каждое согласие принадлежит одному клиенту; у клиента может быть несколько записей согласий (по типам и по истории).

---

### `Сотрудник`

**Назначение.** Сотрудник (управляющий/владелец, охранник КПП и др.) с учётной записью в системе: используется для входа в админку/интерфейс КПП и привязки действий/уведомлений к конкретному пользователю персонала.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор сотрудника/учётной записи | *число* | Формат UUID | Да | Концептуальная модель, UI админки |
| Логин | логин | login | Имя пользователя для входа | *текст* | Длина 3–255, уникальность (предложение) | Да | UI, требования к аутентификации |
| Пароль (хэш) | парольХэш | passwordHash | Хэш пароля учётной записи | *текст* | Длина хэша по выбранному алгоритму (предложение) | Да | Технические требования (auth) |
| Роль | роль | role | Роль сотрудника: охранник, управляющий, владелец | <охранник, управляющий, владелец> | Значение из Enum | Да | Контекстная диаграмма, UI |
| Фамилия | фамилия | lastName | Фамилия сотрудника | *текст* | Длина 1–50 (предложение: непустая, разумная длина ФИО) | Да | UI, кадровые данные |
| Имя | имя | firstName | Имя сотрудника | *текст* | Длина 1–50 (предложение: непустая, разумная длина ФИО) | Да | UI, кадровые данные |
| Отчество | отчество | middleName | Отчество сотрудника | *текст* | Длина 0–50 (предложение: отчество может отсутствовать) | Нет | UI, кадровые данные |
| Телефон | телефон | phone | Контактный телефон | *текст* | Длина ровно 12; формат: `+7XXXXXXXXXX`, где `X` — цифра 0–9 (предложение) | Нет | Интервью, UI |
| E-mail | email | email | Служебный e‑mail | *текст* | Формат email: обязательный символ `@`, до `@` минимум 1 символ, после `@` минимум 3 символа и хотя бы одна точка `.` в доменной части; длина 5–255 (предложение) | Нет | Интервью, UI |
| Статус | статус | status | Активен, заблокирован | <активен, заблокирован> | Значение из Enum | Да | Бизнес‑правила |
| Секрет TOTP (зашифрованно) | секретTotpЗашифрованно | totpSecretEncrypted | Секрет TOTP для второго фактора аутентификации (2FA); хранится в зашифрованном виде, привязка к учётной записи сотрудника | *текст* | Зашифрованное хранение; доступ только для операций проверки кода (предложение) | Нет | Артефакт «Аутентификация и авторизация» |

**Связи:**

- `Сотрудник` —(1:0..N)→ `Обращение` — сотрудник (охранник) обрабатывает обращения клиентов.
- `Сотрудник` —(1:0..N)→ `Уведомление` (опционально) — сотрудник может инициировать формирование нуля или многих уведомлений; каждое уведомление может быть инициировано не более чем одним сотрудником.
- `Сотрудник` —(1:0..N)→ `Парковочная сессия` (опционально) — сотрудник может быть указан как принявший решение о ручном допуске/коррекции для нуля или многих сессий.
- `Сотрудник` —(0..1:1..N)→ `Клиент` (опционально) — сотрудник может быть указан как последний изменивший статус для нуля или многих клиентов; у каждого клиента может быть не более одного такого сотрудника.

---

### `Настройки уведомлений`

**Назначение.** Настройки клиента по типам уведомлений: каналы доставки и включение/отключение уведомлений по парковочным сессиям, бронированиям, договору, оплате и маркетинговым рассылкам. Юридические согласия (согласие с офертой, ПДн, маркетинг) хранятся в отдельной сущности `Согласие`.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -------------- | --------------------------- | --------------------- | ------------------------------------------------------------------------- | ------------------------- | ----------- | ---------- | -------------------------------- |
| Идентификатор | идентификатор | id | Уникальный идентификатор настроек уведомлений | *число* | Формат UUID | Да | Концептуальная модель |
| Каналы | каналы | channels | Каналы, по которым клиент согласен получать уведомления (личный кабинет, звонок, e‑mail, SMS) | <множество: личный кабинет, звонок, e‑mail, SMS> | Подмножество значений Enum (предложение) | Нет | Контекстная диаграмма, протоколы |
| ПС | уведомленияПарковочнойСессии | parkingSessionEnabled | Признак включения уведомлений по парковочной сессии (въезд/выезд и т.п.) | *логический* | true/false | Нет | Контекстная диаграмма, протоколы |
| Бронирование | уведомленияБронирования | bookingEnabled | Признак включения уведомлений по бронированиям | *логический* | true/false | Нет | Контекстная диаграмма, протоколы |
| Договор | уведомленияДоговора | contractEnabled | Признак включения уведомлений по договору | *логический* | true/false | Нет | Контекстная диаграмма, протоколы |
| Оплата | уведомленияОплаты | paymentEnabled | Признак включения уведомлений по оплате (списание, возврат, неуспех) | *логический* | true/false | Нет | Контекстная диаграмма, протоколы |
| Маркетинг | маркетингВключён | marketingEnabled | Признак включения маркетинговых уведомлений (наличие согласия проверяется по сущности `Согласие`) | *логический* | true/false | Нет | Контекстная диаграмма, протоколы |

**Связи:**

- `Настройки уведомлений` ←(1:1)— `Клиент` — у клиента ровно одни настройки уведомлений; каждая запись относится к одному клиенту.

---

### `Настройки оплаты`

**Назначение.** Настройки клиента по автосписанию и лимитам. Привязанные способы оплаты (карты, кошельки) хранятся на стороне внешнего платёжного сервиса; система хранит только идентификатор плательщика в этом сервисе для инициации списаний и запросов методов оплаты.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| ------------------------------------------ | --------------------------- | --------------------- | ------------------------------------------------------------------------- | ------------------------- | ----------- | ---------- | -------------------------------- |
| Идентификатор | идентификатор | id | Уникальный идентификатор настроек оплаты | *число* | Формат UUID | Да | Концептуальная модель |
| Идентификатор плательщика во внешнем ПС | внешнийПлательщикИд | externalPayerId | Внешний идентификатор плательщика (токен/ссылка) в платёжном сервисе; по нему провайдер возвращает привязанные способы оплаты и выполняет списания | *текст* | Длина ≤ 100 (предложение) | Нет | Интеграция с платёжной системой |
| Автосписание Договор | автосписаниеДоговор | autoDebitContract | Признак включения автосписания по договору | *логический* | true/false | Нет | Контекстная диаграмма, протоколы |
| Автосписание ПС | автосписаниеПарковочнойСессии | autoDebitParkingSession | Признак включения автосписания по парковочным сессиям | *логический* | true/false | Нет | Контекстная диаграмма, протоколы |
| Лимит в месяц | лимитВМесяц | monthlyLimit | Максимальная сумма автосписаний в месяц (ограничение для клиента) | *число* | ≥ 0 (предложение: неотрицательная сумма) | Нет | Контекстная диаграмма, протоколы |

**Связи:**

- `Настройки оплаты` ←(1:1)— `Клиент` — у клиента ровно одни настройки оплаты; каждая запись относится к одному клиенту.

---

### `Клиент`

**Назначение.** Субъект, получающий услуги парковки (ФЛ или ЮЛ).

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор клиента | *число* | Формат UUID | Да | Концептуальная модель |
| Тип | тип | type | ФЛ / ЮЛ (источник истины для типа клиента; используется всеми связанными сущностями) | <ФЛ, ЮЛ> | Значение из справочника | Да | Контекстная диаграмма |
| Организация | организацияИд | organizationId | Юридическое лицо, если клиент является ЮЛ | <Ссылка на Организация> | внешний ключ или null | Да (для ЮЛ) | Контекстная диаграмма, интервью |
| Паспортные данные | паспортныеДанныеИд | passportDataId | Паспортные данные клиента (для ФЛ) | <Ссылка на Паспортные данные> | внешний ключ или null | Нет | Требования по идентификации |
| Фамилия | фамилия | lastName | Фамилия клиента (для ФЛ) | *текст* | Длина ≤ 100 (предложение) | Да (для ФЛ) | Контекстная диаграмма |
| Имя | имя | firstName | Имя клиента (для ФЛ) | *текст* | Длина ≤ 100 (предложение) | Да (для ФЛ) | Контекстная диаграмма |
| Отчество | отчество | middleName | Отчество клиента (для ФЛ) | *текст* | Длина ≤ 100 (предложение) | Нет | Контекстная диаграмма |
| Телефон | телефон | phone | Основной контактный телефон | *текст* | Длина ≤ 20 (предложение: протокол — контактные данные от всех клиентов) | Да | Контекстная диаграмма |
| E-mail | email | email | Основной контактный e‑mail | *текст* | Формат email: обязательный символ `@`, до `@` минимум 1 символ, после `@` минимум 3 символа и хотя бы одна точка `.` в доменной части; длина 5–255 (предложение) | Нет | Контекстная диаграмма |
| Льготный документ | льготныйДокументИд | benefitDocumentId | Документ, подтверждающий право клиента на льготу (удостоверение, справка, карта и т.п.) | <Ссылка на Льготный документ> | внешний ключ или null | Нет | Контекстная диаграмма, требования по льготам |
| Настройки уведомлений | настройкиУведомленийИд | notificationSettingsId | Ссылка на настройки уведомлений клиента (каналы и типы уведомлений, согласия) | <Ссылка на Настройки уведомлений> | внешний ключ на существующую запись | Да | Контекстная диаграмма, протоколы |
| Настройки оплаты | настройкиОплатыИд | paymentSettingsId | Ссылка на настройки оплаты клиента (автосписание, лимиты, привязанные методы оплаты) | <Ссылка на Настройки оплаты> | внешний ключ на существующую запись | Да | Контекстная диаграмма, протоколы |
| Пароль (хэш) | парольХэш | passwordHash | Хэш пароля для аутентификации клиента в личном кабинете | *текст* | Длина хэша по алгоритму (предложение) | Да (для сценариев с ЛК) | Технические требования (auth) |
| Статус | статус | status | Активен, архив, заблокирован, в ЧС и т.п. | <активен, архив, заблокирован> | Значение из Enum | Да | Бизнес‑правила |
| Причина статуса | причинаСтатуса | statusReason | Краткое текстовое пояснение причины текущего статуса (например, задолженность, нарушение правил, решение службы безопасности) | *текст* | Длина ≤ 500 (предложение) | Нет | Бизнес‑правила, протоколы |
| Сотрудник | последнееИзменениеСотрудникомИд | lastModifiedByEmployeeId | Сотрудник, последним изменивший статус клиента | <Ссылка на Сотрудник> | внешний ключ или null | Нет | Бизнес‑правила, UI админки |
| Идентификатор субъекта у провайдера SSO | идентификаторСубъектаУПровайдераSso | ssoSubjectId | Идентификатор субъекта у провайдера SSO (sub); для связи учётной записи клиента ФЛ с IdP при входе через Яндекс ID / VK ID | *текст* | Длина ≤ 255 (предложение) | Нет | Артефакт «Аутентификация и авторизация» |
| Провайдер SSO | провайдерSso | ssoProvider | Код провайдера SSO (Яндекс ID, VK ID) для клиента ФЛ при входе через IdP | *текст* или <справочник> | Значение из справочника (Яндекс ID, VK ID) (предложение) | Нет | Артефакт «Аутентификация и авторизация» |

**Связи:**

- `Клиент` ←(1:0..1)— `Организация` (для ЮЛ) — клиент‑ЮЛ ссылается на организацию или не имеет её; каждая организация может быть связана не более чем с одним клиентом.
- `Клиент` —(1:0..1)→ `Паспортные данные` (для ФЛ) — у клиента‑ФЛ может быть ноль или одна запись паспортных данных.
- `Клиент` —(1:1)→ `Настройки уведомлений` — у клиента ровно одни настройки уведомлений.
- `Клиент` —(1:1)→ `Настройки оплаты` — у клиента ровно одни настройки оплаты.
- `Клиент` —(1:0..N)→ `ТС` — у клиента может быть ноль или несколько транспортных средств.
- `Клиент` —(1:0..N)→ `Договор` — у клиента может быть ноль или несколько договоров.
- `Клиент` —(1:0..1)→ `Льготный документ` — у клиента может быть ноль или один льготный документ.
- `Клиент` —(1:0..N)→ `Согласие` — у клиента может быть ноль или несколько записей согласий (по типам и в истории).
- `Клиент` —(1:0..N)→ `Обращение` — обращение всегда принадлежит какому‑то клиенту.
- `Клиент` —(1:0..N)→ `Уведомление` — уведомления адресуются клиенту.

---

### `ТС` (Транспортное средство)

**Назначение.** Транспортное средство клиента, для которого оформляются доступ, бронирования и сессии.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор ТС | *число* | Формат UUID | Да | Концептуальная модель |
| Клиент | клиентИд | clientId | Владеющий клиент | <Ссылка на Клиент> | внешний ключ на существующую запись | Да | Концептуальная модель |
| ГРЗ | госНомер | licensePlate | Государственный регистрационный знак | *текст* | Длина ≤ 12 (предложение: формат РФ по ГОСТ Р 50577 — 1 буква + 3 цифры + 2 буквы + регион) | Да | Контекстная диаграмма, СКУД |
| Тип/категория ТС | типТсИд | vehicleTypeId | Тип/категория транспортного средства (легковой, мотоцикл, муниципальная техника и т.п.), задаваемая через справочник типов ТС | <Ссылка на Тип ТС> | внешний ключ на существующую запись | Да | Контекстная диаграмма |
| Марка | марка | brand | Марка | *текст* | Длина ≤ 50 (предложение) | Нет | Интервью |
| Модель | модель | model | Модель | *текст* | Длина ≤ 50 (предложение) | Нет | Интервью |
| Цвет | цвет | color | Цвет автомобиля | *текст* | Длина ≤ 30 (предложение) | Нет | Интервью |

**Связи:**

- `ТС` ←(0..N:1)— `Клиент` — транспортные средства принадлежат клиенту.
- `ТС` —(1:0..N)→ `Бронирование` — бронирования создаются для конкретного ТС.
- `ТС` ←(0..N:1)— `Тип ТС` — каждое транспортное средство имеет один тип/категорию из справочника типов ТС.

---

### `КПП`

**Назначение.** Контрольно‑пропускной пункт (шлагбаум/ворота) въезда/выезда.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор КПП | *число* | Формат UUID | Да | Концептуальная модель |
| Парковка | парковкаИд | parkingId | Принадлежность парковке | <Ссылка на Парковка> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Наименование | наименование | name | Внутренний код / обозначение (север/юг и т.п.) | *текст* | Длина ≤ 50 (предложение) | Да | Схема парковки AS‑IS |
| Тип | тип | type | Въезд, выезд, двусторонний | <въезд, выезд, двусторонний> | Значение из Enum: Тип КПП | Да | Контекстная диаграмма |
| Статус | статус | status | Работает, не работает, обслуживание и т.п. | <справочник> | Значение из справочника | Нет | Операционная модель |

**Связи:**

- `КПП` ←(1..N:1)— `Парковка` — каждый КПП принадлежит одной парковке; у парковки может быть один или несколько КПП.
- `КПП` —(1:0..N)→ `Парковочная сессия` — один КПП может выступать точкой въезда и/или выезда для множества сессий или не использоваться.

---

### `Тариф`

**Назначение.** Правила расчёта стоимости (ставки, интервалы, льготы).

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор тарифа | *число* | Формат UUID | Да | Концептуальная модель |
| Наименование | наименование | name | Название тарифа | *текст* | Длина ≤ 255 (предложение) | Да | Контекстная диаграмма |
| Тип | тип | type | Почасовой, суточный, абонемент и т.п. | <справочник> | Значение из Enum: Тип тарифа | Да | Impact Map, протоколы |
| Льготная категория | льготнаяКатегория | benefitCategory | Для каких льготных категорий применяется | <справочник> | Значение из справочника или null | Нет | Контекстная диаграмма |
| Ставка | ставка | rate | Базовая ставка | *число* | ≥ 0 (предложение) | Да | Бизнес‑правила |
| Шаг тарификации | шагТарификации | billingStep | Интервал времени (15 мин, 1 час и т.п.) | *текст* | > 0 (предложение: положительный интервал) | Да | Бизнес‑правила |
| Максимальная сумма | максимальнаяСумма | maxAmount | Ограничение по максимальной сумме | *число* | ≥ 0 при заполнении (предложение) | Нет | Интервью |
| Грейс‑период | грейсПериод | gracePeriodMinutes | Максимальное время (в минутах) от момента успешной оплаты или окончания оплаченного периода до выезда, в течение которого повторная тарификация не производится | *число* | ≥ 0 (предложение: неотрицательное целое, минуты; 0 — без грейс‑периода) | Нет | Бизнес‑правила тарификации, ES TO‑BE |

**Связи:**

- `Тариф` —(1:0..N)→ `Бронирование` — тариф применяется при расчёте стоимости бронирований.
- `Тариф` —(1:0..N)→ `Применимость тарифа к типу зоны` — один тариф может быть применим к нескольким типам зон через записи применимости.

---

### `Обращение`

**Назначение.** Запрос/жалоба/вопрос клиента к сотруднику парковки.

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор обращения | *число* | Формат UUID | Да | Концептуальная модель |
| Клиент | клиентИд | clientId | Ссылка на клиента | <Ссылка на Клиент> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Сотрудник | сотрудникИд | employeeId | Ответственный сотрудник/охранник, обрабатывающий обращение | <Ссылка на Сотрудник> | внешний ключ или null | Нет | Концептуальная модель, протоколы |
| Тип | тип | type | Вопрос, жалоба, претензия, предложение | <вопрос, жалоба, претензия, предложение> | Значение из Enum | Да | Интервью, форма ЛК |
| Канал | канал | channel | Канал поступления (ЛК, звонок, e‑mail и т.п.) | <личный кабинет, звонок, e‑mail, SMS> | Значение из Enum | Да | Контекстная диаграмма |
| Тема | тема | subject | Краткое описание темы | *текст* | Длина 5–200 (предложение: исключить односимвольные темы) | Да | Интервью |
| Описание | описание | description | Подробное описание | *текст* | Длина 20–1000 (предложение: минимум для осмысленного текста, максимум ~½ страницы A4) | Да | Интервью |
| Тип связанного объекта | типСвязанногоОбъекта | relatedEntityType | Тип сущности, к которой относится обращение (Бронирование/Платеж/Чек/Договор/Парковочная сессия и т.п., опционально) | <справочник> | Значение из Enum (предложение) | Нет | Концептуальная модель |
| Связанный объект | связанныйОбъектИд | relatedEntityId | Уникальный идентификатор связанного объекта указанного типа | *число* | Уникальный идентификатор записи в сущности, указанной в `типСвязанногоОбъекта` (предложение) | Нет | Концептуальная модель |
| Статус | статус | status | Новое, в работе, ожидает клиента, решено, отклонено, закрыто | <новое, в работе, ожидает клиента, решено, отклонено, закрыто> | Значение из Enum | Да | Бизнес‑правила |

**Связи:**

- `Обращение` ←(N:1)— `Клиент` — каждое обращение принадлежит клиенту.
- `Обращение` ←(N:1)— `Сотрудник` — обращение обрабатывается конкретной учётной записью сотрудника (например, охранника).
- `Обращение` ←(N:1)— `Бронирование` / `Парковочная сессия` / `Платеж` / `Чек` / `Договор` (опционально) — обращение может быть привязано к конкретному объекту; связь реализуется через атрибуты `типСвязанногоОбъекта` и `связанныйОбъектИд` (полиморфная ссылка).
- `Обращение` —(1:N)→ `Уведомление` (опционально) — уведомления по обращениям.

---

### `Шаблон уведомления`

**Назначение.** Образец темы и тела сообщения для уведомлений по типу события. Содержит плейсхолдеры (имя клиента, номер брони, сумма и т.д.), подставляемые при отправке. Канал доставки задаётся в экземпляре `Уведомление`. Управление шаблонами — в админке (Реестр Use Case UC-6.x: создать, просмотреть, изменить, удалить, список шаблонов).

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| --------------| ---------------------------| ---------------------| -------------------------------------------------------------------------| ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор шаблона уведомления | *число* | Формат UUID | Да | Концептуальная модель |
| Код | код | code | Внутренний код шаблона (например, `BOOKING_CREATED_SMS`, `PAYMENT_OVERDUE_EMAIL`) | *текст* | Длина 3–50, уникальность (предложение: формат UPPER_SNAKE_CASE) | Да | Интеграция с сервисом уведомлений |
| Наименование | наименование | name | Человекочитаемое название шаблона | *текст* | Длина ≤ 255 (предложение) | Нет | Интервью, админка |
| Тип | тип | type | Тип события: по договору, по оплате, по доступу, по бронированию, по парковочной сессии и т.п. | <по договору, по оплате, по задолженности, по доступу, по бронированию, по парковочной сессии, по обращениям, маркетинг, иное> | Значение из Enum | Да | Контекстная диаграмма |
| Тема | тема | subject | Шаблон темы/заголовка (с плейсхолдерами) | *текст* | Длина 5–200 (предложение: исключить односимвольные темы) | Нет | Интервью |
| Тело | тело | body | Шаблон тела сообщения (с плейсхолдерами) | *текст* | Длина ≤ 5000 (предложение) | Да | Интервью |

**Связи:**

- `Шаблон уведомления` —(1:0..N)→ `Уведомление` — по одному шаблону формируется ноль или много уведомлений; каждое уведомление может ссылаться не более чем на один шаблон.

---

### `Уведомление`

**Назначение.** Сообщение, отправляемое клиенту о событиях в системе. Содержит ссылку на шаблон, получателя, канал и статус доставки; тема и тело при отображении или повторной отправке формируются по шаблону с подстановкой данных из связанного объекта. Типы уведомлений и каналы (SMS, e‑mail, push) согласованы с Контекстной диаграммой и интеграцией «Сервис уведомлений» (в сервис: уведомление, канал, адресат; из сервиса: статус отправки).

| Атрибут | Атрибут (camelCase, рус.) | Атрибут (camelCase) | Описание | Тип / домен | Ограничение | Обязателен | Источник |
| -----------------------| ---------------------------| ---------------------| ----------------------------------------------- | ------------------------------------| ----------- | ---------- | --------------------------------|
| Идентификатор | идентификатор | id | Уникальный идентификатор уведомления | *число* | Формат UUID | Да | Концептуальная модель |
| Шаблон уведомления | шаблонУведомленияИд | notificationTemplateId | Шаблон, по которому сформировано уведомление | <Ссылка на Шаблон уведомления> | внешний ключ или null | Нет | Концептуальная модель |
| Клиент | клиентИд | clientId | Получатель уведомления | <Ссылка на Клиент> | внешний ключ на существующую запись | Да | Концептуальная модель |
| Канал | канал | channel | Канал, по которому отправлено (личный кабинет, e‑mail, SMS) | <личный кабинет, e‑mail, SMS> | Значение из Enum | Да | Интеграция с сервисом уведомлений |
| Статус доставки | статусДоставки | deliveryStatus | Результат доставки уведомления | <успешно, ошибка> | Значение из Enum | Да | Интеграция с сервисом уведомлений |
| Сотрудник | сотрудникИд | employeeId | Сотрудник, по действию которого было инициировано уведомление (например, изменение статуса клиента, ручные операции) | <Ссылка на Сотрудник> | внешний ключ или null | Нет | Протоколы, бизнес‑правила |

**Связи:**

- `Уведомление` ←(N:1)— `Клиент` — уведомления адресуются клиенту и отправляются по его контактным данным.
- `Уведомление` ←(N:1)— `Шаблон уведомления` (опционально) — уведомление может быть сформировано по шаблону или вручную.

---

## Связи (обобщенный список)

Ниже приведены основные связи между сущностями концептуальной модели с указанием кардинальностей. Они действуют как для базовой концептуальной модели, так и для варианта с атрибутами в данном документе.

- **`Парковка` —(1:N)→ `Сектор`** — структурирование территории на зоны/этажи.
- **`Сектор` —(1:N)→ `ПМ`** — размещение конкретных парковочных мест в секторе.
- **`Парковка` —(1:N)→ `КПП`** — у парковки несколько точек въезда/выезда.
- **`КПП` —(1:0..N)→ `Парковочная сессия`** — КПП может выступать точкой въезда и/или выезда для множества сессий или не использоваться.
- **`ПМ` —(1:0..N)→ `Бронирование`** — одно место может многократно бронироваться в разные периоды (для долгосрочных/договорных бронирований); краткосрочные бронирования могут ссылаться только на сектор.
- **`Клиент` —(1:0..N)→ `ТС`** — у клиента может быть ноль или несколько транспортных средств.
- **`Клиент` —(1:1)→ `Настройки уведомлений`** — у клиента ровно одни настройки уведомлений.
- **`Клиент` —(1:1)→ `Настройки оплаты`** — у клиента ровно одни настройки оплаты.
- **`Клиент` —(1:0..N)→ `Согласие`** — у клиента может быть ноль или несколько записей согласий (по типам и в истории).
- **`Клиент` —(1:0..N)→ `Договор`** — у клиента (ФЛ/ЮЛ) может быть ноль или несколько договоров; долгосрочные бронирования оформляются в рамках договора клиента.
- **`Договор` —(0..1:1..N)→ `Бронирование`** — один договор может иметь ноль или много связанных бронирований; каждое долгосрочное бронирование (>24 часов) может быть привязано не более чем к одному действующему договору.
- **`Бронирование` —(0..1:0..N)→ `Парковочная сессия`** — из одного бронирования может не возникнуть ни одной или возникнуть несколько сессий; в реализуемом варианте каждая сессия должна иметь привязку к бронированию (в т.ч. созданному при въезде).
- **`ТС` —(1:0..N)→ `Бронирование`** — бронирования создаются для конкретного ТС.
- **`Тариф` —(1:0..N)→ `Бронирование`** — тариф применяется при расчёте стоимости бронирований.
- **`Бронирование` —(0..1:0..N)→ `Платеж`** — бронирование может оплачиваться одним или несколькими платежами либо не иметь платежей.
- **`Платеж` —(1:0..1)→ `Чек`** — успешный платеж порождает не более одного фискального чека; при неуспешном платеже чек не формируется.
- **`Шаблон договора` —(1:0..N)→ `Договор`** — по одному шаблону может быть сформировано ноль или много договоров.
- **`Договор` ←(0..N:1)— `Шаблон договора`** — договор может быть сформирован по шаблону или без него.
- **`Шаблон уведомления` —(1:0..N)→ `Уведомление`** — по одному шаблону формируется ноль или много уведомлений.
- **`Уведомление` ←(N:1)— `Шаблон уведомления`** (опционально) — уведомление может быть сформировано по шаблону или вручную.
- **`Обращение` ←(N:1)— `Клиент` / `Сотрудник`** — обращение всегда принадлежит клиенту и обрабатывается сотрудником.
- **`Обращение` ←(N:1)— `Бронирование` / `Парковочная сессия` / `Платеж` / `Чек` / `Договор` (опционально)** — обращение может быть привязано к конкретному объекту; связь реализуется через атрибуты `типСвязанногоОбъекта` и `связанныйОбъектИд`.
- **`Уведомление` ←(N:1)— `Клиент`** — уведомления адресуются клиенту и отправляются по его контактным данным.

Дополнительные связи, специфичные для этого документа:

- **`Согласие` ←(N:1)— `Клиент`** — каждое согласие принадлежит одному клиенту.
- **`Тип зоны` —(1:N)→ `Сектор`** — каждый сектор имеет один тип зоны.
- **`Тип зоны` —(1:N)→ `Разрешённый тип ТС в типе зоны` —(N:1)→ `Тип ТС`** — задаёт, какие типы ТС разрешены для каждого типа зоны.
- **`Организация` —(0..1:1)→ `Клиент`** — организация может быть привязана к одному клиенту типа ЮЛ или не использоваться.
- **`Тип зоны` —(1:N)→ `Применимость тарифа к типу зоны` —(N:1)→ `Тариф`** — задаёт, какие тарифы применимы к каким типам зон.
- **`Тип ТС` —(1:N)→ `ТС`** — каждый тип/категория ТС может быть назначен множеству транспортных средств.

## Согласование с контекстом проекта (уведомления)

В Контекстной диаграмме явно указаны типы: **по задолженности** и **ответы по обращениям**. В перечислении `Enum: Тип уведомления` не хватало этих значений.

**Сделано в концептуальной модели:**

- В **Enum: Тип уведомления** добавлены значения: `по задолженности` и `по обращениям` (и зафиксировано соответствие Контекстной диаграмме и протоколам).
- В назначении сущности **Уведомление** добавлена фраза о согласованности с Контекстной диаграммой и интеграцией «Сервис уведомлений».

## Дальнейшее развитие модели

- Уточнить атрибуты по результатам следующих протоколов интервью и согласований с заказчиком.
- Для атрибутов с типом "Структура" и "Полиморфная ссылка" разработать отдельные логические схемы.
- На следующем шаге перенести модель в логический уровень (ER‑диаграмма) с нормализацией и уточнением ключей.

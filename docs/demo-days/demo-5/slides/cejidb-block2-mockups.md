# Макеты слайдов — Блок 2 · Артем

Визуальные макеты четырех слайдов блока 2 (~4 мин).
Источник содержания — [demo-5-script.md](../demo-5-script.md#артем-cejidb--блок-2), тайминг и крюки — там же.

---

## S1 · USM + MVP (~80 сек)

**Заголовок:** «MVP: что берем в первый релиз, а что честно не обещаем»
**Крюк:** что берем в первый релиз, а что честно не обещаем — USM как общий язык с бизнесом.

```mermaid
flowchart LR
    classDef mvp   fill:#e3f2fd,stroke:#1565c0,color:#0d47a1
    classDef out   fill:#ffebee,stroke:#c62828,color:#7f1d1d
    classDef crit  fill:#c8e6c9,stroke:#2e7d32,color:#1b5e20
    classDef foot  fill:#f5f5f5,stroke:#9e9e9e,color:#424242

    subgraph USM["Левая часть · USM — 5 рабочих областей, ~200 историй"]
        direction TB
        U1["✅ 1. Клиентское самообслуживание<br/>тарифы · регистрация · профиль · ТС · уведомления"]:::mvp
        U2["✅ 2. Оформление услуги<br/>бронирование · договор · оплата"]:::mvp
        U3["✅ 3. ДОПУСК НА ПАРКОВКУ<br/>идентификация · автовъезд-выезд"]:::crit
        U4["✅ 4. Операционное управление<br/>схема · статусы мест · уведомления"]:::mvp
        U5["❌ 5. Аналитика<br/>протокол №4: «не нужна на релизе»"]:::out
    end

    subgraph TABLE["Правая часть · Таблица «Область → решение по MVP»"]
        direction TB
        T1["✅ Клиентское самообслуживание — целиком"]:::mvp
        T2["✅ Оформление услуги — ФЛ;<br/>ЮЛ-онлайн — управляющий (протокол №4)"]:::mvp
        T3["✅ ДОПУСК НА ПАРКОВКУ — целиком<br/>★ выделен крупнее: ручная идентификация + очередь"]:::crit
        T4["✅ Операционное управление — целиком"]:::mvp
        T5["❌ Аналитика — честно позже"]:::out
    end

    FOOT["user-story-map.md · mvp-definition-of-done.md"]:::foot

    U3 -.->|"ключевая точка"| T3
    TABLE --- FOOT
```

---

## S2 · Контекстная диаграмма (~85 сек)

**Заголовок:** «Граница системы — это решение про состав поставки и интеграции»
**Крюк:** где граница системы и какие интеграции снимают ручную идентификацию.

```mermaid
flowchart TB
    classDef internal fill:#e3f2fd,stroke:#1565c0,color:#0d47a1
    classDef critical  fill:#ffebee,stroke:#c62828,color:#7f1d1d
    classDef sso       fill:#fff3e0,stroke:#ef6c00,color:#5a2800
    classDef other     fill:#f5f5f5,stroke:#bdbdbd,color:#424242

    subgraph SYSTEM["СИСТЕМА · Цифровая платформа парковки"]
        direction LR
        APP1["Business Site<br/>сайт ФЛ / ЮЛ"]:::internal
        APP2["Consumer App<br/>ЛК клиента"]:::internal
        APP3["Admin Panel<br/>управляющий"]:::internal
        APP4["Custom Internal Tool<br/>внутренний инструмент"]:::internal
        GUARD["Интерфейс охранника КПП"]:::internal
    end

    I1["СКУД + LPR<br/>ручная идентификация:<br/>авторасп. ГРЗ"]:::critical
    I2["Платежный шлюз<br/>онлайн + терминал"]:::critical
    I3["ОФД<br/>фискализация"]:::critical
    I4["Сервис уведомлений<br/>клиент · охранник · управляющий"]:::critical

    SSO["SSO — только ФЛ<br/>КПП + Админка:<br/>логин + 2FA TOTP"]:::sso
    OTHER["+10 прочих интеграций<br/>ЭДО · биллинг · аналитика · ..."]:::other

    I1   <-->|LPR-событие / статус| SYSTEM
    I2   <-->|платеж / статус| SYSTEM
    I3   <-->|чек| SYSTEM
    I4   <-->|push / SMS / email| SYSTEM
    SSO  -.->|только ФЛ| SYSTEM
    SYSTEM --- OTHER
```

---

## S3 · Концептуальная модель (~60 сек)

**Заголовок:** «Сущности домена: бронирование отделено от факта стоянки»
**Крюк:** общий язык для требований Дениса и архитектуры mrneatly.

```mermaid
flowchart LR
    classDef entity fill:#e8eaf6,stroke:#3949ab,color:#1a237e
    classDef key    fill:#fff3e0,stroke:#ef6c00,color:#5a2800
    classDef rule   fill:#e8f5e9,stroke:#388e3c,color:#1b5e20
    classDef foot   fill:#fffde7,stroke:#f9a825,color:#5a3d00

    subgraph MODEL["Левая часть · Концептуальная модель (уменьшенная схема)"]
        direction TB
        CLI["Клиент"]:::entity
        VEH["ТС"]:::entity
        TAR["Тариф"]:::entity
        BKG["Бронирование"]:::entity
        SES["Парковочная сессия"]:::entity
        PAY["Платеж / чек"]:::entity
        CON["Договор"]:::entity
        SLT["Парковочное место"]:::entity

        CLI --> BKG
        CLI --> CON
        VEH --> BKG
        TAR --> BKG
        BKG --> SES
        SLT --> SES
        SES --> PAY
    end

    subgraph KEY["Правая часть · Ключевое решение модели (крупно)"]
        direction TB
        K1["Бронирование — мастер-сущность:<br/>ТС · сектор/ПМ · тариф · сумма<br/>платежи · чеки · договорные ограничения"]:::key
        K2["Парковочная сессия — факт пребывания:<br/>въезд · выезд · КПП · длительность<br/>автоматический или ручной допуск"]:::key
        K3["Платеж / чек / договор —<br/>вокруг Бронирования, не вокруг Сессии"]:::key
        K4["Правило: въезд без брони →<br/>система создает бронирование на лету;<br/>Сессия всегда ссылается на него"]:::rule
    end

    FOOT["«Это решение держит вместе требования Дениса и DDD-контексты mrneatly»"]:::foot

    KEY --- FOOT
```

---

## S4 · UC: диаграмма + реестр + 1 пример (~90 сек)

**Заголовок:** «От концепции — к сценарию на въезде»
**Крюк:** 4 расширения UC-12.1 — ручная идентификация становится автоматическим решением, охранник не импровизирует.

```mermaid
flowchart LR
    classDef reg    fill:#e8f5e9,stroke:#2e7d32,color:#1b3a1c
    classDef main   fill:#e3f2fd,stroke:#1565c0,color:#0d47a1
    classDef ext    fill:#fff3e0,stroke:#ef6c00,color:#5a2800
    classDef result fill:#ffebee,stroke:#c62828,color:#7f1d1d

    subgraph REG["Левая треть · Реестр UC"]
        direction TB
        R1["16 групп<br/>~190 UC<br/>UC-1.1 — UC-16.3"]:::reg
        R2["[миниатюра<br/>диаграммы UC]"]:::reg
        R3["use-case-registry.md"]:::reg
        R1 --- R2 --- R3
    end

    subgraph UCFLOW["Правая 2/3 · UC-12.1 «Автоидентификация на въезде» · Акторы: СКУД, ИС парковки"]
        direction TB
        M1["1. Событие от СКУД — LPR-скан"]:::main
        M2["2. Поиск ТС по ГРЗ"]:::main
        M3["3. Поиск активного бронирования"]:::main
        M4["4. Запись в журнал доступа"]:::main
        M5["5. Табло: «Доступ разрешен»"]:::main
        M6["6. Открыть шлагбаум"]:::main
        M7["7. → UC-12.4: создание парковочной сессии"]:::main

        M1 --> M2 --> M3 --> M4 --> M5 --> M6 --> M7

        E1A["1а: ГРЗ не распознан<br/>→ ручная идентификация"]:::ext
        E2A["2а: ТС не найдено<br/>→ Табло: «добавьте ТС в ЛК»"]:::ext
        E3B["3а2а: задолженность<br/>→ Табло: «погасите»"]:::ext
        E3C["3а2б: черный список<br/>→ к охране"]:::ext

        M1 -.->|"1а"| E1A
        M2 -.->|"2а"| E2A
        M3 -.->|"3а2а"| E3B
        M3 -.->|"3а2б"| E3C
    end

    BOTTOM["Ручная идентификация → автоматическое решение.<br/>Очередь на КПП → быстрые правила исключений."]:::result

    UCFLOW --- BOTTOM
```
